# Open Differentials — Cozy Arcade Browser Test Log
**Format:** Never delete rows. Close items by changing status + adding commit. Append new rows as Codex finds them.
**Last updated:** 2026-06-24 | SW PHASE2 v51 | SW PHASE1 v87

---

## STATUS KEY
- ✅ CONFIRMED FIXED — browser-validated with commit
- ⚠️ MITIGATED — partially addressed, residual risk noted
- ❌ OPEN — confirmed by browser, not yet fixed
- 🔍 MONITORING — not confirmed in browser yet, worth watching
- ⬛ BY DESIGN — intentional behavior, not a bug
- ✗ DISPROVED — static analysis claim proven wrong by browser

---

## RENDER — Timer / Drop Engine

| ID | Status | First Found | Browser Evidence | Description | Fix |
|----|--------|------------|-----------------|-------------|-----|
| FQ-RENDER-1 | ✅ 948abe7 | 2026-06-15 audit | selectSolo fired twice, ~42ms apart; both CSS vars `--soloDropY` and `--soloDropY351` animated | System 2 (`raf175164`) and System 3 (`soloStableRaf351`) ran simultaneously; `selectSolo` fired twice per card; all 11 wrapper layers executed twice | System 2 tick expiry now checks `choiceRow.classList.contains('soloStableDrop351')` before calling selectSolo. Confirmed: SS#1 once from System3 line 6985 (PHASE2) / 7017 (PHASE1) |
| FQ-RENDER-1-A | ✗ DISPROVED | 2026-06-15 static | — | Static claim: System 0 `DROP_MS=7000` hardcoded was cause of premature auto-select | Browser showed selectSolo fired at ~13s with 13s setting → System 0 is NOT the live timer; System 3 (`soloStableRaf351`) is |
| FQ-RENDER-1-B | ✗ DISPROVED | 2026-06-16 attempt | Codex stack trace: SS#1 still from System2 | Attempt: `clearSoloDrop()` called from stable mode IIFE → believed to cancel System2 | `clearSoloDrop()` is IIFE-scoped in System2; call from stable mode throws ReferenceError silently |
| FQ-RENDER-1-C | ✗ DISPROVED | 2026-06-16 attempt | Codex stack trace: SS#1 still from System2 | Attempt: reassign `loopSolo` at end of SS351 → believed to prevent System2 restart | System2 renderSolo calls `startDrop()` DIRECTLY, not via loopSolo; reassignment had no effect |
| D2 | ✅ 948abe7 | 2026-06-16 design | — | 3 RAF handles (`raf`/`raf175164`/`soloStableRaf351`), only 2 cancel functions; `clearSoloDrop` only kills `raf175164` | Mitigated via DOM class guard; System0 raf handled separately by `stopAllDropTimers` |
| D3 | ⚠️ mitigated | 2026-06-16 design | — | Timer drift: System 0 `DROP_MS=7000` hardcoded; System 3 reads localStorage; both fire at 7s ±10ms causing near-simultaneous expiry | System 2 silenced by DOM guard; System 0 not the live timer; residual: if DOM guard fails, both fire again |
| FQ-RENDER-4 | 🔍 deferred | 2026-06-15 audit | Static analysis only | 700ms debounce at selectSolo layer 11 (outermost) — outer 10 wrappers bypassed it when dual-fire occurred; undo + rating outer layers fired twice per card | Resolved by FQ-RENDER-1 fix (single fire). Structural: debounce should be at outermost call site. Revisit if dual-fire recurs. |
| DOM-GUARD-FRAG | 🔍 monitoring | 2026-06-17 audit | Not reproduced, theoretical | DOM class guard depends on `choiceRow.soloStableDrop351` being present before System2 expires (~7s). If a future render clears `choiceRow.className` after stable start, System2 could fire again | No fix needed now. If recurs: expose System2 cancel handle on `window` or use explicit ownership flag |
| SILENT-CATCH | 🔍 monitoring | 2026-06-17 audit | Not reproduced | System2 guard wraps in `try/catch(e){}` — if `q`, `choiceRow`, or classList logic breaks, code silently returns without selecting (card stuck) | Long-term: replace with explicit ownership flag (`window.__cozyStableOwnsTimer=true`) instead of DOM class check |
| DOMAIN-TIMER | 🔍 monitoring — now browser-exercised, one new finding (2026-06-24) | 2026-06-17 audit; first real browser exercise 2026-06-24 Codex retest | Domain mode (`renderDomain`/`loopDomain`/`orbArena`) finally exercised in a real browser. No regression in the timer/render path itself: normal click via Playwright timed out after 3s because the orb never reached a stable position, forced click worked and reveal rendered correctly. | **New finding, not the original DOMAIN-TIMER risk:** Domain orb's normal click is unstable under automation — possible touch-target/moving-target UX risk on real devices, not confirmed user-facing (forced click works, content renders correctly once clicked). Not yet actioned — candidate fix direction is reducing orb motion on pointerdown or expanding the stable hit zone, but needs a real-device/manual-tap check before treating as a real bug, same caution as `LIVE-NO-DECK`'s "automation result is not always the user's result." |
| FQ-RENDER-5 | ✅ FIXED 2026-06-19 (5th attempt, this one validated) | 2026-06-18 found; fixed via CODEX_PROMPT_10 | `window.__cozyStableOwnsSoloTimer351` flag, checked at the top of System2's `tick()` and `startDrop()` (both now early-return + clear the warning + null the raf handle), reset to `false` every card in System2's `renderSolo()` (stuck-flag safety net), set `true` at the earliest synchronous point Codex found (the `oldRenderSolo351` wrapper, before the `setTimeout(startStableSoloDrop351,0)` deferral — eliminates the race my original prompt only mitigated) and again inside `startStableSoloDrop351()` itself. No new wrapper added. Codex validated locally: FSRS 17/17, smoke 6/6, 3 consecutive Solo auto-select/reveal cycles, 0 AUTO-SELECT mutations, both repos, before push. PHASE2 `fb09afa` / PHASE1 `2c8f4ce`, SW v31→v32 / v66→v67, pushed + live. | System2's `tick()` (PHASE2 line 3906 / PHASE1 ~3918) runs its full RAF loop indefinitely once a card renders — nothing actually stops it. `stopAllDropTimers()` (line 879) only cancels System0's `raf`. The `clearSoloDrop()` call inside `startStableSoloDrop351()` (line 6951) is IIFE-scoped to System2 and throws ReferenceError, silently swallowed by try/catch — it has never worked. The 948abe7 fix only guarded the final `selectSolo()` call at tick's end (line 3924); every earlier line in tick() (warning text/class toggle, drop position, timerFill) runs unconditionally regardless of `soloStableDrop351`. System2's `renderSolo()` (line 3938) unconditionally calls its own `startDrop()` on every card — System3 never replaces `renderSolo`, it only layers `startStableSoloDrop351()` alongside via `setTimeout(...,0)`, so both engines' loops run in parallel on every single card, not just at the margins. | Proposed (see CODEX_PROMPT_10, not yet applied): one explicit ownership flag (`window.__cozyStableOwnsSoloTimer351`), checked at the top of both `startDrop()` AND `tick()` in System0 and System2 (belt-and-suspenders for the renderSolo-synchronous-call vs. setTimeout(0)-stable-engage race). Reset to `false` at the top of `renderSolo()` every card (safety net so a stuck flag can never freeze the game with no timer at all). Set `true` inside `startStableSoloDrop351()`. No new wrapper layer — modifies the 4 existing functions in place. |
| DOMAIN-AUTO-SELECT | ✅ 0d12676 | 2026-06-17 audit | **Browser-confirmed 2026-06-18 with seeded deck:** timer reached 0%, selectDomain() fired once, record() fired once, reveal/rating buttons appeared on desktop and mobile. | Later `loopDomain` wrapper at PHASE2 line ~7009 overwrites base `loopDomain` (line 413). Base has `if(timer<=0&&autoSelect){...selectDomain(nearest)}`. Wrapper clears ticker at 0 but omits auto-select. | loopDomain wrapper now calls `selectDomain(nearest)` at timer≤0. PHASE2 0d12676 / PHASE1 65ddcdf |

---

## RENDER — Bionic / Write Churn

| ID | Status | First Found | Browser Evidence | Description | Fix |
|----|--------|------------|-----------------|-------------|-----|
| FQ-RENDER-3 | ✅ 8a22e66 | 2026-06-15 audit | 3 innerHTML write events per card; last write = plain text stripping `<b>` | `installBionicQuestionPatch352` re-wrote `soloQuestion.innerHTML` as plain text AFTER bionic pass. Three writers: base renderSolo + System0 + System2. All used closure-captured `bionic()` instead of patched `window.bionic` | All soloQuestion writes now use `(window.bionic||bionic)()`. Added `<b>` guard to installBionicQuestionPatch352 in PHASE1. Final HTML confirmed `<b>` present. |
| D1 | ✅ 8a22e66 | 2026-06-16 design | — | Closure capture: System 0/2 `bionic` = old ref captured at IIFE definition; `window.bionic` updated later by `installBionicQuestionPatch352` | Fixed by `(window.bionic\|\|bionic)()` |
| D4-MUTATION | ⚠️ REOPENED 2026-06-19 — was "not blocking," user now reports it directly | 2026-06-15 audit; user-reported recurrence 2026-06-19 (screen recording + 5 screenshots, `claude_small_screen_recording_2026-06-19.mp4` + `claude_screenshots_low_jpeg/`) | 8 MutationObserver callbacks per card (not 8 distinct writes); write count still 3 in 2026-06-17 audit. **2026-06-19 user report: "card glitch/flashing," reproduced on a complete new browser/computer with zero cache, during JSON import.** Claude reviewed the 5 screenshots + 1 extracted video frame (no ffmpeg available to inspect actual motion) — stills show consistent, correct content (same Sarcoidosis/Löfgren reveal card throughout, no text corruption/duplication); the visible difference between screenshot groups looks like a browser window/zoom change (toolbar/zoom-badge differs), not an app-level bug, but a real rapid flicker can be invisible in stills and only visible in motion. Session was at Gate 12 (not the first card), so this is NOT exclusively a first-import-only issue if it's the same mechanism. **Not confirmed as the same root cause as the original 2026-06-15 finding — could be MutationObserver write-churn, could be something specific to zero-cache/fresh-profile timing, not yet distinguished.** | **Not patched blind — could not be confirmed from static evidence.** See CODEX_PROMPT_14 (diagnostic only, live browser, explicitly test under a clean/incognito profile to match the user's zero-cache repro condition; capture rapid DOM mutations Claude's stills can't show). **STILL OPEN 2026-06-24: user reconfirmed flashing after today's HUD-ICON-BLANK/OLD-SETTINGS-LEAK fix landed — that fix (icon-paint + #gearBtn-hide CSS only, `b571eb3`/`80cf287`) is confirmed unrelated, do not treat as fixed-by-association. Live instrumentation is the next step, not another static guess: `CODEX_PROMPT_17` expanded same day to also watch `#soloQuestion`/`#choiceRow`/`.promptBox` (the live question card, not just `#soloReveal`) and log target selector + old/new text + stack per mutation. **Codex ran the expanded `PROMPT_17` 2026-06-24 against both repos with a real uploaded deck + iPhone viewport — found the real mechanism: 19 separate `keydown` listeners exist in PHASE2 (same count independently confirmed in PHASE1); for Enter/Space during a Solo reveal, at least 5 independent paths can each call `advance()` on one keystroke (`advanceReveal`@3178, `continueReveal`@1279/3278/6810, `clickContinue`@5653, base handler@432's direct call) because `stopImmediatePropagation` is globally shimmed to a no-op during reveal-open (FQ-ALGO-9). Three of those paths already have their OWN local debounce lock (`revealAdvanceLockUntil`, `revealClickLock161`) but none share state, so cross-path double-firing wasn't prevented. ⚠️ MITIGATED (not closed) 2026-06-24, PHASE2 `562facd` / PHASE1 `0057270`, **NOT YET LIVE-VALIDATED**: added one guard at the single confirmed funnel all paths already call through — `wrappedAdvance`, reusing the existing `__cozyLastAdvanceAt351` timestamp (already live-validated by FQ-ALGO-9) as one shared 350ms lock. User explicitly chose this lowest-risk option over a full 19-listener consolidation, since Claude has no browser to validate a larger change. Next step: Codex re-run the same `PROMPT_17` harness against this fix — confirm mutation counts drop and no regression to legitimate rapid double-Space. Do not mark D4-MUTATION/REVEAL-TRIGGER-CHURN closed until that retest comes back. |
| REVEAL-TRIGGER-CHURN | ⚠️ MITIGATED 2026-06-19 (Claude, ~5:00pm) — root cause NOT consolidated, only the worst symptom guarded | 2026-06-19 Codex pre-mortem harness (browser-confirmed: 55/57 mutations) | Localhost browser harness with `MutationObserver` on `#soloReveal`: 55 reveal-subtree mutations for a normal card; 57 for a board-trigger-only card. `.boardTrigger350` and `.oneThing350` are created after reveal starts; `renderRevealSections()` is called via `setTimeout(...,0)` and every 900 ms while reveal is open. | Live reveal rendering has too many owners with divergent source priorities: base reveal uses `educational_objective || board_trigger || quick_recall`; later wrapper uses `answer || educational_objective || level_2 || quick_recall`; later renderer rewrites diagnosis/trigger/board/One Thing and mutates structure after paint. | **Partial fix applied** (PHASE2 `7bf4273` / PHASE1 `b91cf8f`, ~5:00pm 2026-06-19): `renderRevealSections()`'s `dx`/`trigger` writes now skip when content signature is unchanged (mirrors the existing `oneThingStableRenderSig` guard) — removes the redundant rewrite at this one (last, deferred) writer. **Not done:** the 900ms interval still runs, the other ~16 chain layers still each write unconditionally, `.boardTrigger350`/`.oneThing350` structural insertion-after-paint is untouched. Full consolidation into one idempotent owner (Codex's recommendation) deliberately not attempted today — too large a change to risk without live validation. **Codex post-fix validation (~5:20pm, same day):** confirmed the *settled* state is now correct (board-trigger-only card shows board_trigger, not diagnosis, within ~20ms) — the fix works as designed. But the *earliest* synchronous writers in the chain (base `reveal()` line 415, the wrapper at 1265-1269) still run before `renderRevealSections` and paint the uncorrected/contaminated content first — so a brief first-frame flash of wrong content remains before the correction lands ~20ms later. Mutation count only dropped slightly (55→53 / 57→55) since the other ~16 layers are untouched. **Resolved 2026-06-19 ~6:00pm (PHASE2 `57a8f0e` / PHASE1 `b69fa21`):** tracing "the earliest writer" turned out to require finding the actual live one — the chain has 17 reassignments but most are dead. `window.reveal=reveal175158` (~line 1878) is a hard replacement with no call to any prior, and is ITSELF superseded by another hard replacement at line ~7125 (also no prior call). Confirmed line ~7125 is the true live root: the only reassignments after it (8913/9380/9718) all properly chain via `prior.apply()`. Applied the same contamination guard there (line ~7141, the actual first content the user sees). Also applied to base `reveal()` (~412) and the ~1265 wrapper for source consistency, though tracing confirms neither is reachable in the live chain — harmless, not load-bearing, left in rather than spending more turns reverting inert code. **Not live-browser-validated this round** — recommend a re-test to confirm the first-frame flash is actually gone now, same as the original fix needed Codex's validation to confirm. The 900ms interval, the other ~16 chain layers, and `.boardTrigger350`/`.oneThing350` structural insertion-after-paint remain untouched — still real, still deferred, still not urgent. **Update 2026-06-23, real browser test confirms stale cross-card content, not just churn:** Codex tested the live Upload→Start Solo→answer→Space flow and found, after advancing, `#soloRevealDx` still showed the PREVIOUS card's diagnosis (Pulmonary embolism) while `#soloTrigger`/`.boardTrigger350` had already updated to the NEW card (Celiac) — a genuine mixed-card display bug, not just redundant mutation. Root cause: nothing in the live `advance()` chain (`wrappedAdvance`, the confirmed convergence point) ever clears or hides the reveal panel when moving to the next card — it only handles rating, then calls through to the base `advance()`/`renderSolo()`. **Fixed:** added an explicit clear-and-hide step in `wrappedAdvance`, after the rating logic captures the outgoing card's state but before the next card renders — wipes `dx`/`trigger`/`.boardTrigger350` text and their idempotency-guard dataset keys, and re-hides the reveal panel, so no stale content from card N can ever bleed into card N+1's display regardless of which upstream writer would otherwise have left it stale. Did NOT attempt the deeper "answered card identity" fix Codex also recommended (`reveal()` capturing the answered card explicitly) — that requires touching `selectSolo`'s own ~11-layer chain and introducing new cross-script state, exactly the "add another wrapper" anti-pattern Codex's own feedback warned against; deferred to `CODEX_PROMPT_17`'s live instrumentation. Mutation count (84 measured by Codex post-board-guard) likely still high — this fix targets the stale-content correctness bug, not the churn-count metric; re-measure, don't assume both are resolved by the same change. **Update 2026-06-24, Codex full local Chrome retest against `main` 2ff95d2/v49 (explicitly superseding the v46/`fee324a` handoff):** Re-measured mutation count at **89** for one normal reveal — still high, root architecture (multi-writer, no single idempotent owner) remains unconsolidated, this stays the top-ranked open item. Three specific 2026-06-22 findings did **not** reproduce this round: duplicate board-trigger rendering (one `.boardTrigger350`, one occurrence), the "Gate Completed"→"Learning Moment" title flip (title stayed `GATE COMPLETED`), and visibly-stale dx after Space (confirmed fixed — previous-card diagnosis does not stay visible post-advance, consistent with the `wrappedAdvance` clear-and-hide fix above holding). **Do not read "did not reproduce" as "fixed for good"** — this round used a normal card under one specific flow; the 06-22 findings were on a board-trigger-only card / longer dwell, conditions not identical. **New, more precise finding:** hidden reveal content still mutates to the next card's trigger/board text after Space while the panel is hidden — not user-visible right now, but proves the reveal chain is not fully inert post-advance, i.e. the clear-and-hide fix stops the symptom without removing the underlying multi-writer cause. **Methodology correction for future instrumentation:** `window.current` was observed null/absent while the rendered question/choices were active and correct (not merely "disagreeing" as the 06-22 finding framed it) — `window.current` is not a reliable validation signal on its own; bind future diagnostics to rendered DOM + fixture IDs, or instrument the card object at selection time, not `window.current`. Gates held: `runFSRSValidation()` 17/17, `runCozySmokeTests()` 6/6, no page errors. Next step is `CODEX_PROMPT_17`'s live instrumentation (queued, not yet run) — goal is one idempotent reveal owner, or at minimum suppressing writes once reveal has settled. Do NOT add another reveal wrapper. **Update 2026-06-24, end-to-end probe with a real uploaded deck at iPhone viewport (Codex):** mutation counts measured far higher than the same-day desktop figure above — **PHASE2: 493 on reveal, 620 after Space advance; PHASE1: 457 on reveal, 589 after Space advance.** Don't read this as a regression from the "89" figure logged earlier today — conditions differ (real uploaded deck vs. whatever fixture the desktop pass used, iPhone viewport vs. desktop, and this measures two separate moments — at-reveal and post-advance — rather than one snapshot), so the numbers aren't directly comparable; both are real measurements under different conditions, not competing claims. Visible stale diagnosis after Space remains cleared (consistent with the `wrappedAdvance` fix holding), but hidden reveal trigger/board content still mutates toward the next card post-advance in both repos — reconfirms REVEAL-TRIGGER-CHURN as the correct top-priority real bug, not cosmetic, regardless of which exact count is used. **✅ ROOT CAUSE FOUND AND FIXED 2026-07-04 (PHASE2 sw v66 / PHASE1 sw v101) — the "multi-writer reveal architecture" theory this whole investigation was built on was only PART of the picture; the majority of measured churn came from something nobody had instrumented for.** Built a real MutationObserver + stack-trace attribution harness (Playwright, real Upload-button flow, not synthetic state injection) against local v65/v100 to identify exactly which function wrote to `#soloReveal`'s subtree, how often, and from where — the kind of live instrumentation this differential had been waiting on since `CODEX_PROMPT_17` was queued 2026-06-24. **Found: two independent, previously-undocumented `setInterval` sweeps run for the entire page lifetime regardless of game state** — `relabelMainFilters()` (1200ms) and `pinLanguage()` (1000ms), each walking `document.querySelectorAll('option,button,span,small,div,label')` (the WHOLE document, not scoped to any screen) and unconditionally reassigning `.textContent` on every childless element, even when the computed replacement is byte-identical to the current text (a leftover "Flagged→Pinned" rebrand sweep, duplicated by two separate unaware patches). Because reveal-panel elements stay in the DOM tree even when hidden via CSS, these sweeps rewrite `#soloTrigger`, `#soloRevealDx`, `.boardTrigger350`, `.oneThing350`, every rating button, etc. every single cycle, whether or not reveal is open. Additionally found two more unconditional (no idempotency check) writers specifically active during reveal: `ensureFull()`'s Full-Card-button label (line ~6789, PHASE2) and `refreshPinButtons()`'s Pin/Unpin label (line ~7100, PHASE2) — both called every 250ms via an existing `setInterval` that already runs `ensureBoard`/`ensureFull`/`refreshPinButtons` while reveal is open, but neither of those two specific lines had `ensureBoard`'s existing `dataset.v350Key` idempotency guard. **Fix: added a write-if-changed guard to all four** (`if(next!==el.textContent) el.textContent=next` / `if(btn.textContent!==label) btn.textContent=label`) — the exact same idempotency-guard pattern already used successfully elsewhere in this file (`revealSectionsSig`, `oneThingStableRenderSig`, `boardSig`). Zero behavior change for the case where text actually needs updating; the only difference is skipping a no-op reassignment when it doesn't. **Measured result, live-instrumented before/after, both repos:** PHASE2 reveal-open phase 58→10 writes (-83%), post-advance phase 49→12 (-76%); PHASE1 reveal-open 141-baseline-equivalent-window→11, post-advance→12. What remains after the fix is exactly the legitimate paint operations (one `reveal()` call, one `renderRevealSections()` correction pass, the bionic refresh, the advance clear-step) — no more redundant same-value rewrites. **Validated both repos before proposing:** `runFSRSValidation()` 17/17, `runCozySmokeTests()` 6/6, full local advance matrix (correct+Space, wrong+Space cycles) — `current` genuinely changes card each time, reveal opens and closes correctly, zero page errors. **Committed and pushed same day** (PHASE2 `c013ff6`, PHASE1 `49475dc`), live-confirmed via curl (PHASE2 v66, PHASE1 v101) and re-verified the churn reduction holds on production itself, not just locally (10/12 writes on live PHASE2, matching the local measurement exactly). **Stale-content-while-hidden question (open since 2026-06-23/24) re-tested directly against live production, same day:** advanced from card "Bravo" to card "Charlie" and inspected `#soloTrigger`/`.boardTrigger350` DOM content immediately after advance and again after a 1.7s settle (long enough for the 250ms/300ms/1000ms periodic refresh loops to fire multiple times) — both showed Charlie's correct board_trigger/EO text throughout, zero trace of Bravo's diagnosis text. **No stale cross-card drift reproduced post-fix.** Not a full guarantee across every card-type/timing combination (board-trigger-only cards, longer dwell times, etc. per the original 06-22 caveat), but a real, direct, live re-test of the exact concern, not an assumption. **What this fix did NOT do:** consolidate the underlying "no single idempotent reveal owner" architecture — the `reveal()` chain's 17 reassignments (most dead code) are exactly as messy as before. This fix eliminated the noise that was making the real signal hard to see; it didn't touch the chain itself. |
| REVEAL-FIRST-FRAME | ❌ OPEN — browser-confirmed after mitigation | 2026-06-19 Codex post-Claude validation | Re-running the same in-page browser harness after PHASE2 `7bf4273`: board-trigger-only contaminated card still shows `Aortic dissection` in `#soloTrigger` immediately after `reveal('solo', true)`, then corrects to `Tearing chest pain + pulse deficit -> immediate CTA chest.` by ~20ms. | The mitigation is in deferred `renderRevealSections()`, not the earliest visible reveal writer. Therefore the first frame can still paint contaminated text before the final writer corrects it. This is exactly the browser-surfaced kind of glitch the user can perceive as flash. | Next safe fix: move the corrected reveal source/fallback into the earliest visible reveal writer or create a shared reveal model used before the panel is unhidden. Do not touch SM-2/rating. See `CODEX_POST_CLAUDE_REVEAL_FIX_VALIDATION_2026-06-19.md`. |
| RENDER-STR-INVALID | ✗ DISPROVED | 2026-06-17 audit | `renderSoloStringHasStable:false` in browser | `String(window.renderSolo).includes('startStableSoloDrop351')` used as validation gate | **REMOVED from all validation gates.** Later wrappers hide inner stable reference. Always false at runtime. |
| FQ-RENDER-2 | ✅ line 3939 | 2026-06-15 audit | Body className observed going empty and returning on each card advance | `document.body.className=''` at System 2 render path (~line 3939) dropped layout classes | Save/restore `cozy*`/`na-*`/Drawer classes around the clear |
| DOMAIN-BIONIC | ✅ f345dda | 2026-06-15 audit | Static only | `domainQuestion` bionic writes (lines 410/445 PHASE2) still use closure `bionic()`. FQ-RENDER-3 scope was solo only. | Applied `(window.bionic\|\|bionic)()` at PHASE2 lines 410, 445 / PHASE1 lines 409, 444. SW v27/v62. PHASE2 f345dda / PHASE1 3dcbe0a |
| RENDER-STALE-REVEAL | 🔍 monitoring | 2026-06-19 — CODEX_PROMPT_11 live browser | Codex observed reveal text retaining/mixing previous and next card's objective text after `advance()`, in at least one captured state. **Not confirmed user-visible** — Codex did not see it as displayed on screen, only in captured DOM text content, so may be corrected before paint. Lower priority than DOMAIN-AGAIN-DUPE/FQ-RENDER-5. Revisit after both are fixed — if still present, instrument exact timing of `reveal()`'s text writes vs. `advance()`'s next-card render. | Not actioned. |
| RENDER-NULL-CURRENT | 🔍 monitoring, confirmed real via forced test, NOT naturally reproduced | 2026-07-04 — Claude found via Playwright repro during the `ADVANCE-LOCK-SELF-CANCEL` emergency investigation; Codex live-tested natural vs. forced paths same day | `renderSolo()` reads `current.system` (PHASE2 ~line 3953, PHASE1 ~line 3964) with no null-guard on `current` itself. **Confirmed via forced browser test** (`current=null; renderSolo()`): throws `Cannot read properties of null (reading 'system')` uncaught, both repos — this would abort the render mid-way, before the line that hides the reveal panel, leaving the UI looking stuck exactly like `ADVANCE-LOCK-SELF-CANCEL`'s symptom. **Codex's live natural-path test (single-card and multi-card real decks, 4+ full cycles): no console error, no crash** — `current` never actually goes null under normal play. Originally surfaced by Claude's own synthetic 1-card test repro, where the pool was trivially exhausted after one answer — an artificial condition, not proof of a real-world trigger. | Not actioned — correctly deferred per Codex's explicit call: this is a genuine defensive-coding gap (worth a low-risk null-guard fix eventually) but is NOT the cause of any confirmed user-facing incident. Do not patch reactively; wait for either a natural browser path producing `current===null` or a real live console error matching this exact signature before touching it. |
| DOMAIN-WRITER-ORDER | ✗ DISPROVED | 2026-06-17 static | **Browser-confirmed 2026-06-18 NOT present:** 3 domainQuestion.innerHTML writes, all preserved bionic `<b>` output. Medical terms (Strongyloides, Moderate, Anchored) intact. Later writes at lines ~6687/~7449 use `bionicHTML351()` or `patched(prompt)` — both bionic-safe (not raw text). | — |

---

## ALGORITHM — FSRS Scheduling

| ID | Status | First Found | Browser Evidence | Description | Fix |
|----|--------|------------|-----------------|-------------|-----|
| FQ-ALGO-1 | ✅ 048d073 | 2026-06-15 | Again→10m ✅, Hard→1d ✅, Good→3d ✅, Easy→15d ✅ | Anonymous `setTimeout(rateCard(id,'good'), 8000)` in base selectSolo overwrote explicit Again at 8.2s | Timer stored as `window.__cozyAutoRateHandle20260603`; `cozy-explicit-rating-stabilizer-2026-06-11` wraps rate/rateCard to cancel on explicit rating |
| FQ-ALGO-2 | ⬛ BY DESIGN | 2026-06-15 | — | `repair_point=true` on wrong answers + E7G immediate-due pool → again cards appear in Review Deck before 10-min due time | Intentional. May need configuring if user finds it jarring. |
| FQ-ALGO-3 | ✅ 0d12676 | 2026-06-15 | `isDue()=true` always for these 18 cards; every session feels accelerated. Clean browser context showed 0 null rows — bug exists only in user's persisted localStorage, not reproducible with seeded test deck. | 18 review-stage rows with `next_due_at=null`; always due | One-time repair block after `loadPhase3State()`: sets `next_due_at=now`, `interval_days||=1` for review-stage null-due cards; saves only if changed>0. Uses local `phase3State.progress`. PHASE2 0d12676 / PHASE1 65ddcdf |
| FQ-ALGO-4 | ✅ 22260dc | 2026-06-15 | — | "Again" cards: old splice to pool[0] was ineffective — nextCard uses pool[index%length], never picks pool[0] until index wraps all 104 cards | Pool-rebuild fix: rateCard('again') sets session.poolKey=''; session.pool=[] → sessionPool() rebuilds with repair_point=1 sort + index=0 reset → again card picked next. isDue() returns true for repair_point (line 11135) so review_deck includes card despite next_due_at=+10min. PHASE2 22260dc / PHASE1 3a921fc |
| FQ-ALGO-5 | 🔍 open | 2026-06-15 | — | Wrapper accumulation: rating-path-rectifier + explicit-stabilizer both reinstall `rate()` at overlapping timeouts; layered chains on rate() | Functionally idempotent; architectural risk only. Monitor. |
| FQ-ALGO-6 / K | ❌ OPEN | 2026-06-15 | — | `wrong_count` conflates lane accuracy + self-rating (explicit Again) — inflated wrong counts | Deferred |
| RUNNER-INTERCEPTS-CLICKS | ✅ FIXED and live-browser-validated 2026-06-24 (Codex local Chrome retest, `main` 2ff95d2 / v49) | 2026-06-23 — Claude, confirmed by Codex's iPhone-size (375x667) probe finding `#runner` over choice centers, `pointer-events:auto` | Root cause confirmed: `#domainRunner162` (domain mode) and `#runner.hunter > *` (runner's children) already had `pointer-events:none`, but `#runner.hunter` itself never did — sat at `z-index:40!important`, above `.choiceRow`'s `z-index:24/31`. No click handler on `#runner` itself (checked all 5 JS references — all reads/positioning, none `addEventListener('click',...)`), so disabling its pointer-events is safe. | Fixed both repos: one CSS rule, `#runner.hunter{pointer-events:none!important}`, end of file. PHASE2 `43f6a93` (sw v49), PHASE1 `450c084` (sw v85). **Live-confirmed 2026-06-24:** re-tested at both 390x844 and 375x667 — `#runner` computed `pointer-events:none` at both sizes, choice tap centers not blocked. Closed — do not re-fix.
| FQ-ALGO-9 | ✅ FIXED AND LIVE-BROWSER-VALIDATED 2026-06-20 (PHASE2 `a5906a7` / PHASE1 `4ea0a13`) — closed for the reproduced symptom, broader architecture risk explicitly NOT closed | 2026-06-20 — Codex re-ran the exact 5-cycle Space-at-reveal reproduction against the fix in both repos: `postAdvanceNewCardSelectCount: 0` every cycle, both repos. FSRS 17/17, smoke 6/6. Observed `sinceAdvance` at the guard was ~3-4ms in practice (vs. the 50ms threshold — large margin). | **Root cause confirmed, exact mechanism (not just "plausible"):** `Event.prototype.stopImmediatePropagation` is overridden (PHASE2 line ~5876) to silently no-op for `KeyboardEvent` on Enter/Space while a reveal is open — Claude verified this shim directly. Deliberate-looking (no comment explaining why), presumably so multiple reveal-key handlers can coexist. Side effect: a window-level handler (~line 5662) correctly detects reveal-open and calls `advance()` for Enter/Space, calls `e.stopImmediatePropagation()` to stop further handlers — but because the shim neuters that call for exactly this key/condition, propagation continues. The base bubble-phase handler (~line 432, fires dead last) then runs for the SAME event, now sees the reveal as closed (since `advance()` already ran and rendered the next card), and fires `selectSolo(selected)` on a card the user never saw. Listener count was also undercounted: Codex found 20 in PHASE2, 21 in PHASE1 (Claude had documented 19; the discrepancy doesn't change the fix). | Did not touch the global shim — unknown blast radius across 20+ listeners, no comment explaining its other 2 conditions (domain-mode arrows, Escape), too broad a change for what's needed. Instead: `wrappedAdvance` (the single point every `advance()` call funnels through regardless of which of the 20 listeners triggered it) now stamps `window.__cozyLastAdvanceAt351=performance.now()` at its very start. Line 432's ambiguous "reveal closed → selectSolo" branch now skips if an advance happened within the last 50ms. Verified via JXA simulation before applying, then confirmed live. **Explicitly NOT claiming the broader architecture is fixed — Codex's own post-validation feedback, agreed with in full:** (1) the `stopImmediatePropagation` shim itself is unchanged and remains a standing risk — any FUTURE stale consumer added to this codebase could reintroduce the same class of bug; (2) the 50ms guard is timing-based, not structural — it has a large empirical margin (observed gap ~3-4ms) but a sufficiently-delayed async path could theoretically still slip past it; (3) line ~5616's unnecessary `selectSolo` call on Enter/Space-while-reveal-open remains (confirmed harmless, still inefficient). None of these three are being actioned now — they're the real cost of choosing the narrow, low-risk fix over touching the shared shim. | **Continue-button flag (from FQ-ALGO-8): mostly answered, not cleanly proven.** Codex's follow-up test cleared `pending`/`lastRated` and clicked literal Continue — result was 'good' in both repos, consistent with the flag path working. Caveat Codex correctly raised: that forced state still double-rated the already-selected wrong card, so this demonstrates the fallback's behavior under an artificial setup, not a guaranteed real-user-flow invariant. Low priority — not pursuing a fully clean version of this test further unless a related issue resurfaces. |
| FQ-ALGO-8 | ⚠️ MITIGATED 2026-06-19 ~6:30pm (PHASE2 `34697f4` / PHASE1 `02e4d23`) — fallback hardened, exact race trigger still not pinned down | 2026-06-19 — user observed directly: "a couple autoselect, space or arrow continue, one wrong autoselected but good" | **Pre-mortem re-trace before fixing:** re-verified `advance()`'s full 7-layer chain with fresh line numbers (today's edits had shifted them) — confirmed, unlike `reveal()`, NONE of the 7 are hard-replacements; every layer genuinely wraps and calls its prior, so the whole chain is live end-to-end. Found a previously-undocumented 5th-of-7 layer (~12184, "E2 fix") that independently checks `window.__cozyLastRatedId` and calls `rateCard(id,'good')` directly — but traced it correctly no-ops whenever select-time rating already succeeded, same as `wrappedAdvance`'s own guard. Ruled out a stale-closure `selectSolo` theory (checked the enclosing `<script id="cozy_v350_rescue_js">` block, lines 5808-7352, containing the live auto-select call site at ~7003 — confirmed no local `selectSolo` declaration shadows the global one anywhere in that block, so the auto-select call does reach the current, rating-aware `wrappedSelect`). **Could not find a deterministic logic bug after this tracing** — every guarded path looks correct in isolation, consistent with the bug being a genuine intermittent timing race (matches the user's own report: "one" wrong rating out of "a couple" autoselects, not every time) rather than a static error. | **Did not guess at the race trigger — hardened the fallback that produces the wrong symptom regardless of why it's reached.** Found the live "Continue" button implementation (`installRatings()`, ~line 7813) calls `advance()` with no rating logic of its own — it relies entirely on `wrappedAdvance`'s `\|\|'good'` fallback, which is *why* that fallback must stay 'good' for explicit Continue clicks (preserves existing, intentional behavior). Added one flag, `window.__cozyExplicitContinueClick351`, set only by the live Continue button's onclick, checked in `wrappedAdvance`: if set, fallback stays 'good' (unchanged behavior); if not set (i.e. advance() was triggered by Space/Arrow dismissing a reveal *without* an explicit Continue click — exactly the user's reported sequence), the fallback now computes the real rating via `ratingForSelection(card, selected)` instead of assuming the best case. Verified via 5-scenario JXA simulation (pending-rating-wins, explicit-Continue-preserves-good-even-on-wrong-answer, Space/Arrow-dismiss-now-computes-real-rating) before applying. Does not touch FSRS math, `rateCard`, or `selectSolo`'s own rating computation. **Not live-browser-validated** — the underlying race that reaches this fallback is still not pinned down, so this is a safety-net fix, not a proven root-cause fix; recommend a re-test focused specifically on reproducing the original repro sequence. |
| DOMAIN-AGAIN-DUPE | ✅ FIXED 2026-06-19 (PHASE1 only — PHASE2 was never affected) | 2026-06-19 — CODEX_PROMPT_11 live Playwright found it; CODEX_PROMPT_12 (instrumented diagnostic, no source edits) found the real mechanism | **Root cause was not `selectDomain`'s ≥13-layer chain** (that chain does fire the rating path repeatedly — measured 5-6x per single click via the `rate()`/`requeueAgainCard` wrapper-reinstall schedule — but firing repeatedly is harmless BY ITSELF). The actual bug: **PHASE1 had an older, additive `requeueAgainCard()`** (`sess.pool.splice(at,0,Object.assign({},card))`, no dedupe) that PHASE2 had already replaced with a non-additive version (just a future-due skip-check + `seenThisSession` cleanup) at some unrecorded prior port. Calling the additive version 5-6 times = 5-6 inserted copies. PHASE2's pool builds correctly (`[3,4,5,6,7,8,9,2]`, no dupes) under the exact same repeated-firing condition — confirming the wrapper-cascade itself is not the bug, just the trigger that exposed PHASE1's stale function. | Ported PHASE2's exact `requeueAgainCard` body into PHASE1, verified via 5x-repeat JXA simulation (both the future-due and not-yet-due branches) before commit. PHASE1 `2b84281`, SW v67→v68, pushed. Not live-browser-validated this round (Codex's diagnostic session ended before a fix; Claude applied directly). **This was the project's first confirmed PHASE1/PHASE2 source divergence from an unrecorded partial port — worth a general one-time PHASE1↔PHASE2 diff audit at some point, not urgent.** |
| FQ-ALGO-5-MEASURED | 🔍 monitoring, upgraded from theoretical to measured | 2026-06-19 — CODEX_PROMPT_12 instrumented diagnostic | FQ-ALGO-5 ("wrapper accumulation... functionally idempotent; architectural risk only") now has real numbers: a single rating click cascades through **5-6 stacked invocations** of the `rate()`/`rateCard()` wrapper chain, almost certainly from `install()`'s repeated re-wrap schedule (`[700,1600,3200,5200,8000].forEach(setTimeout(install,ms))`) each adding a layer rather than detecting prior wrapping. Currently harmless in both repos now that DOMAIN-AGAIN-DUPE is fixed (every handler in the chain is idempotent), but this is unnecessary churn and a latent risk if a future handler in the chain isn't idempotent. | Not actioned — still architectural-risk-only, now with evidence. Lower priority than anything currently open. |
| DOMAIN-RECORD-ZERO | 🔍 monitoring | 2026-06-18 browser | **Browser-confirmed:** after wrong domain auto-select, progress shows `repair_point: true` but `seen_count/reviewed_count/wrong_count = 0`. record() fires but rateCard() is not called until user clicks a rating button. | Possibly intentional (FSRS updates only on explicit rating). Risk: if user leaves domain mode without rating, wrong domain answer leaves no FSRS trace and card never gets scheduled for repair. Flag to user for intent confirmation before fixing. |
| FQ-ALGO-7 | ✅ FIXED (applied directly by Claude, Codex had no credits) | 2026-06-17 regression (0d12676/22260dc), fixed 2026-06-18 | Source-verified: byte-exact revert to pre-22260dc code (confirmed via `git show 22260dc`), confirmed no code added after 22260dc depends on the full-reset behavior (checked all other `session.poolKey`/`session.pool` reset sites: lines 11330/11404/11871/12191, all unrelated triggers), traced splice logic against 4 representative pool scenarios via macOS JXA (`osascript -l JavaScript`) — mid-pool splice, already-at-front no-op, not-in-pool-but-found, not-found-anywhere all behaved correctly. **✅ BROWSER-CONFIRMED 2026-06-19 (CODEX_PROMPT_11, live Playwright against deployed URLs, both repos):** rating a non-front pooled card "Again" moved only that card to front; rest of queue unchanged. PHASE2: `[1,2,3,4...]→Again(3)→[3,1,2,4...]`. PHASE1: identical. **Confirmed not the cause of remaining reported glitches.** | Restored the pre-22260dc splice-to-front logic in `rateCard()`'s again-branch. PHASE2 `c2807ac` / PHASE1 `b9168f5`. SW bumped PHASE2 v30→v31, PHASE1 v65→v66. Pushed 2026-06-19, live on origin/main+public (PHASE2) and origin/main (PHASE1). Closed. |
| FQ-ALGO-7B | ❌ OPEN | 2026-06-18 source audit (logic predates this window — traced to 2026-05-25 b8dc61b, not a recent regression) | Source-confirmed | `isDue(progress)` (line 11130) hard-returns inside `if(progress.next_due_at){...return t<=Date.now();}` whenever next_due_at parses to a valid date. This makes the `repair_point` fallback at line 11137 **unreachable** for any card with a real next_due_at — including every freshly-missed 'again'/'hard' card, since rateCard always sets next_due_at (+10min for again, +interval days for hard). Contradicts this file's own FQ-ALGO-4 entry, which assumed isDue() returns true for repair_point cards. | Missed cards do not actually appear in 'due' or 'review_deck' filters until their literal next_due_at timer elapses, despite repair_point=true and review_deck's sort comparator assuming they're already present. Lower urgency than FQ-ALGO-7 (pre-existing, not new), but means "review deck shows missed cards immediately" has likely never been true. Not actioned yet. |

---

## DATA — Integrity

| ID | Status | First Found | Browser Evidence | Description | Fix |
|----|--------|------------|-----------------|-------------|-----|
| FQ-POOL-1/2 | ✅ 83079db | 2026-06-15 | — | Pinned cards passed `isDue\|\|pinned` every pool pass with no `buriedToday` protection; Shadow Dungeon scopes didn't filter seen/buried | `isSessionBlockedCard()` added; filters applied to all pool scopes + Shadow Dungeon |
| FQ-DATA-1 | ✅ 83079db | 2026-06-15 | — | `repair_point=true` never cleared when answer was correct | `record(ok=true)` now clears `repair_point:false` |
| FQ-DATA-2 | ✅ ACTUALLY FIXED 2026-06-19 — **the 3104391 "fix" was dead code, never closed for real** | 2026-06-15 found; falsely closed 2026-06-17; re-discovered by user's own progress export 2026-06-19 | **User found it via export, not a test gate:** opened `/Downloads/cozy_arcade_progress_2026-06-19.json`, noticed absurd wrong_count on a few cards (incl. Primary Biliary Cholangitis). Claude verified: the 3104391 guard checked `p?.schema_version === 'fsrs5'` — grepped every `schema_version` assignment in the file, that string is never written to a per-card progress object, anywhere. Guard could never be true. `migrateLegacyProgress()` ran its full inflation logic on every page load, every card, adding +1 to wrong_count whenever a card's last_rating was still `'again'`. Quantified against the real export: 27 of 99 reviewed cards (27%) had `wrong_count != reviewed_count - correct_count` (an invariant rateCard() guarantees, since correct/wrong are mutually exclusive increments per rating — neither field has this bug, only wrong_count's extra `+(rating==='again'?1:0)` term does). Worst cases: two cards at wrong_count=136 (true value 1), one at 81 (true value 1) — likely ~135/80 page reloads while stuck in 'again' state. **wrong_count is user-visible** in Neural Atlas card detail (`openCardDetail`, red "Wrong" stat) and in an aggregate dashboard sum — this was a real, visible bug, not cosmetic. | Corrected the guard to check actual phase3-native fields (`seen_count`/`wrong_count`/`reviewed_count` as numbers + a `stage`) instead of a value nothing writes. Added one-time repair block (`window.__cozyWrongCountRepaired`, same pattern as the existing null-due repair) resetting `wrong_count = reviewed_count - correct_count` for already-corrupted cards. Verified via JXA simulation against real corrupted data points from the user's export before applying. PHASE2 `88af09e` / PHASE1 `0b4482a`, pushed + live. **Not live-browser-validated.** The user's already-downloaded export file still shows the old corrupted numbers (it's a frozen snapshot) — will self-correct on next live app load + next export. **Process lesson: a guard-condition fix must be checked against the actual shape of real persisted data before being marked closed, not just reviewed as a diff.** |
| DATA-EO-ALIAS | ⚠️ MITIGATED at display layer 2026-06-19 (Claude, ~5:00pm) — root cause NOT fixed, this is a guard not a repair | 2026-06-19 Codex pre-mortem harness | Synthetic card with `diagnosis`, `board_trigger`, and `one_thing`, but no `educational_objective`, normalized to `educational_objective = "Aortic dissection"` and displayed diagnosis in the Educational Objective box. | **Claude's follow-up grep found this is systemic, not a single bug:** the same "prefer `answer`/diagnosis-like fields over `board_trigger`" anti-pattern recurs in **8 separate places** (PHASE2 lines 1615, 4002-4003, 5413, 6752, 7140, 7648, 7652, 11645/11676 `canonicalizeCard`). `canonicalizeCard`'s contaminating branch is itself only reachable in `mode:'display'`, which is **never called** in the live app (only `mode:'export'` is, at line 11683) — so Codex's specific repro of *that* function required calling it directly, bypassing the normal app flow; the live contamination users actually see comes from one or more of the other 7 reachable sites, not yet individually confirmed which. | **Symptom guarded, not the source:** `renderRevealSections()` (PHASE2 `7bf4273` / PHASE1 `b91cf8f`) now detects `educational_objective === diagnosis` (the exact contaminated-symptom Codex reproduced) and falls back to `board_trigger||quick_recall` at the final display point — fixes what the user actually sees. **Deliberately not touched today:** the other 7 contamination sites, and `canonicalizeCard`'s dead `display` branch (left as-is, still misleading to read, but inert). Fixing all 8 in one pass would be exactly the bulk/quick-patch risk this project has been burned by before (see RECTIFIER_PLAN 2026-06-19 addenda). Treat as a documented backlog item, not urgent — the display-layer guard already prevents the user-visible symptom regardless of which upstream site is the "real" culprit for any given card. |
| SRS-VALIDATION-MISMATCH | ✗ DISPROVED — Claude verified 2026-06-19 ~5:30pm: not a regression, not even the real gate | 2026-06-19 Codex post-Claude validation | Codex ran `runSRSValidation()` (11/17) and reported it as a possible scheduler regression. **`runSRSValidation()` is a different, unrelated, obsolete function from `runFSRSValidation()`** — the project's actual, documented validation gate referenced everywhere else in these docs. Confirmed via `git log -L`: `runSRSValidation` was added in commit `79b75e5` (2026-05-25), commit message literally "**implement window.runSRSValidation — 13 SM-2 assertions**" — written to test the pre-FSRS SM-2 algorithm, before this project migrated to FSRS. It asserts ease-factor-multiplication intervals (e.g. "hard/review/5d → interval 6" = `round(5×1.2)`) and a dynamically-adjusted `ease_factor` (e.g. "again → ease_factor 2.30") — neither of which the current FSRS implementation does: `fsrsNextInterval(S){return Math.max(1,Math.round(S))}` (stability-based, not ease×interval) and `rateCard()` hardcodes `ease_factor: 2.5` always. **The real gate, `runFSRSValidation()`, was extracted and actually executed by Claude via macOS JXA against the exact current source (FSRS_W constants, all 7 fsrs* functions, byte-identical to index.html) — result: 17/17, empirically confirmed, not inferred.** None of today's 5 fixes (FQ-ALGO-7, FQ-DATA-2, DOMAIN-AGAIN-DUPE, reveal-panel) touch any `fsrs*` function. | No action on the algorithm — there is nothing to fix; `runSRSValidation` testing the wrong contract is the only issue, and the user's actual scheduling algorithm (FSRS) is intact and unaffected by anything done today. Recommend (not yet done, asking user first): mark `runSRSValidation` with a code comment noting it's obsolete/pre-FSRS, so it stops causing this exact false alarm for future Codex sessions. Not deleting it without the user's go-ahead. |
| D8 | ❌ OPEN | 2026-06-16 | Source only | 18 null `next_due_at` rows invisible in source; `isDue()=true` always | Same as FQ-ALGO-3; P5 |

---

## ARCHITECTURE — Structural Risks

| ID | Status | First Found | Browser Evidence | Description | Fix |
|----|--------|------------|-----------------|-------------|-----|
| D9-CHAIN | 🔍 monitoring | 2026-06-16 | — | selectSolo chain = 11 layers; each has own cancel logic; not all cross-cancel; adding layer 12 could reintroduce dual-fire | Hard constraint: do NOT add layer 12 |
| D10 | ✅ 948abe7 | 2026-06-16 | — | Both drop engines called `record()` on same card when selectSolo fired twice → double FSRS write | Resolved by FQ-RENDER-1 single-fire fix |
| D6 | ✅ 22260dc | 2026-06-16 | — | sessionPool stale: pool built at session start; again cards don't splice back in same session | Resolved by FQ-ALGO-4 pool-rebuild fix (22260dc) |
| D7 | ⚠️ mitigated | 2026-06-16 | — | `seenThisSession` Set grows all session; new session needed to reset | Mitigated by `isSessionBlockedCard()` (83079db) |
| INSTALL-BURIED | 🔍 monitoring | 2026-06-15 | Static only | `installBuriedPoolFilter` setInterval(120ms) still running after E7 guard flags — may re-wrap pool over time | Not confirmed in browser. Watch for pool behavior regressions. |
| LOOP-DOMAIN | 🔍 monitoring | 2026-06-15 | Static only | Knowledge Expansion (domain) mode has same dual-engine timer pattern as solo — untested in browser | Run domain smoke before P5 |
| APPLY-PROMPT | 🔍 monitoring | 2026-06-15 | Static only | `applyPromptText` always writes plain text first regardless of `bionicOn` state | Not yet browser-confirmed as user-visible |
| STATE-B | ✅ 98b5254 | 2026-06-15 | Screenshot: 0 cards / 151 reviewed / 58 pinned after Atlas/Progress visit + hard reload | Progress button onclick and `showAtlasScreen()` both wrote `{cards: sysMapData}` (stub objects with only qid_unique+sys) to `cozy_arcade_limitless_cards_v1`. On next reload, stubs displaced real cards. Progress key (`cozy_arcade_progress_v1`) was separate and survived. | 3 fixes: (1) Progress handler + showAtlasScreen now write to `cozy_arcade_atlas_sysmap_v1`; (2) `normalizeDeckIdentities()` guards `setAppCards` with `normalized.length>0`. PHASE2 98b5254 / PHASE1 df8c503 |
| PAGES-SOURCE-BRANCH | ⚠️ FALSELY CLOSED 2026-06-24, CORRECTED 2026-07-04 | 2026-06-24 | API-confirmed: PHASE2 repo's `default_branch` was `public` (likely a historical artifact — repo's original default, never switched when `main` became the real working branch); `public`'s file tree was byte-identical to `main` except for lag from the manual mirroring step, so the split had no privacy/curation purpose — both repos are public on GitHub regardless (`private:false`, confirmed via API). The split caused two real incidents already (PAGES-JEKYLL-PHASE2, and `public` drifting behind `main` on 2026-06-21/22). | User switched PHASE2's Pages source from `public` to `main` in repo Settings, matching PHASE1's existing model. Confirmed live: PHASE2 Pages now serves `cozy-arcade-PHASE2-v49` directly from `main`. The `git push origin main:public --force` step is no longer part of the deploy workflow for PHASE2 — update any prompt/doc that still references it. `public` branch left in place (unused), not deleted. **CORRECTION 2026-07-04: this was wrong, or silently reverted, and nobody re-checked it for 10 days.** The "confirmed live" claim above was never re-verified against a live curl after this entry was written — it was trusted at face value while `main` advanced from v49 to v64 with zero live effect, wasting a full day twice (once ~a week before 2026-07-04, then again on 2026-07-04 itself). Direct proof of the real 2026-07-04 state: `git show origin/public:sw.js` matched the live site exactly; `origin/main` did not — Source was NOT actually `main` in practice, whatever this entry's own note says was done in Settings. **Fixed for real 2026-07-04:** user re-did the Settings → Pages → Source → `main` change and this time it was immediately verified live via curl, not just trusted — PHASE2 now serves `cozy-arcade-PHASE2-v64` directly from `main`, confirmed. Full incident writeup and the resulting permanent prevention rules are in `DEPLOY-STALE-GATE` below — read that entry for the current process, and never again treat "Codex/Claude said this Settings change was made" as equivalent to a live curl confirming it. |

---

## CONTENT CORRUPTION

| ID | Status | First Found | Browser Evidence | Description | Fix |
|----|--------|------------|-----------------|-------------|-----|
| PATCH-LANG-MEDICAL | ✅ ca70006 | 2026-06-17 Codex audit | Browser probe: JSON has "Strongyloides", DOM showed "Goodyloides" after reveal/rating. current.diagnosis = Strongyloides in memory; display layer corrupted. | `patchVisibleLanguage()` TreeWalker walks ALL document.body text nodes. `[/Strong/g,'Good']` matched "Strongyloides"→"Goodyloides". Same risk: `/Moderate/g`→"Hard", `/Anchored/g`→"EASY". | Added `\b` word boundaries: `\bStrong\b`, `\bModerate\b`, `\bAnchored\b`. Matches standalone rating labels but not medical compound terms. SW PHASE2 v24→v25 / PHASE1 v59→v60. |
| PATCH-LANG-WALKER | ✅ 0d12676 | 2026-06-17 | Not yet browser-tested | TreeWalker still walks all body text. Word boundaries fix compound-term collision, but standalone medical words (e.g., "Moderate" severity in card text) could still be rewritten. Secondary: older normalizer at ~line 5412 sets `c.answer = educational_objective`, corrupting answer alias for wrappers that treat answer as diagnosis. | Walker loop now skips text nodes whose ancestor matches `#soloQuestion,#soloReveal,#domainQuestion,#domainReveal,#fullCard,[data-card-content]`. Secondary normalizer alias risk still open. PHASE2 0d12676 / PHASE1 65ddcdf |
| BOARD-TRIGGER-TRUNC | ⬛ BY DESIGN | 2026-06-17 | JSON source: board_trigger already ends at "Treatment involves ivermectin or" in source JSON | board_trigger truncation is source data quality, not a browser/render bug. educational_objective has the complete sentence. | Not a code fix. Data quality issue in user's JSON. |
| DECK-FIELD-DUPE-TEST106 | ⬛ BY DESIGN (data), not a code bug — analysis only, no fix applied yet | 2026-06-20 — Claude independently re-checked Codex's report against the raw deck JSON before accepting its top differential | User-reported glitch: Educational Objective and Board Trigger boxes show near-identical text on both the reveal screen and Full Card, screenshotted on the Esophagitis card. Codex's #1 differential (`normalizeLimitlessCard()`'s `first()` fallback chain duplicating content across canonical fields, PHASE2 ~8386) does NOT hold for this card: pulled `/Users/rebekahbetar/Downloads/TEST_106.json` directly and confirmed `educational_objective`, `board_trigger`, AND `quick_recall` are independently, explicitly populated in the SOURCE JSON with near-duplicate paragraphs — `first()` fallback never triggers because none of the fields are empty. Checked the whole 60-card deck programmatically (difflib ratio >0.8): **45/60 cards have educational_objective≈board_trigger, 46/60 have educational_objective≈quick_recall, 33/60 have all three near-identical.** This is the SAME class of issue as the already-closed `BOARD-TRIGGER-TRUNC` (by-design, source data quality), just far more widespread in this specific deck (likely an AI-deck-generation artifact upstream of this app, not introduced by this app). Also re-verified `renderRevealSections()` (current line 8885-8899): the existing DATA-EO-ALIAS guard (`eduRaw!==card.diagnosis`) is working correctly and is NOT what's failing here — that guard only catches `educational_objective === diagnosis` (a different, already-fixed contamination pattern); it has no way to know two non-empty, non-diagnosis fields happen to contain the same paragraph, because from the code's perspective both are valid, intentional content. | **No code changed.** This is a data-quality finding, not a bug fix, per explicit instruction to pre-mortem before touching anything. If the user wants the app to defend against this regardless of cause (cosmetic de-dup: don't show Board Trigger as its own box if it's >80% textually identical to the already-shown Educational Objective), that's a product/UX decision, not a "fix" — flagged for the user to decide, not actioned. |
| KPI-SPLIT-BRAIN | 🔍 confirmed mechanism, not yet a decided fix | 2026-06-20 — code-verified directly, matches Codex's reported "0 cards / 51 reviewed / 15 pinned" incognito screenshot | `updateKpisPhase3()` (~line 11600-11603): `cardCount` reads from the currently loaded deck array (`all.length`), while `reviewedCount`/`pinCount` read from the separate, independently-persisted `phase3State.progress` object (`vals.filter(...)`). These are two different data sources by design (so review history survives a deck swap) — but the UI never distinguishes "no deck loaded, but you have progress history" from "everything is fine," so 0/51/15 is fully consistent with: progress persisted (from an earlier import or earlier tab in the same incognito session — Chrome's "Incognito (2)" badge means 2 tabs sharing one session's temporary storage, not a cross-session leak) while no deck JSON is currently loaded. | Not actioned. Possible future UX fix: show an explicit "Progress loaded, no deck active — upload a deck to continue" state instead of a bare 0. Not done — pending user decision on priority. |
| IMPORT-MERGE-SCORE-DISCARD | 🔍 confirmed mechanism, real and reproducible from source, not yet a decided fix | 2026-06-20 — code-verified directly (line ~11822-11825) | Import/merge (`importObject`'s progress-merge branch) scores existing vs incoming progress per card (`reviewed_count + last_rating*10 + pinned*8 + interval_days>0*5`) and keeps whichever side scores higher (`win = inS > exS ? inc : ex`) — silently, per card, with no notice to the user. This directly explains "unclear if change in status impacted with continue option": if you played a session, got something wrong, exported, then re-imported that export later, any card where the BROWSER's current state already scored higher than the just-imported snapshot keeps the old in-browser values instead of the freshly imported ones — even though the user explicitly chose Import. | Not actioned. This is a real, verified, by-design-but-unsignposted behavior, not a one-off bug. Worth a future fix (e.g., a toast/summary listing how many cards were skipped because existing progress scored higher) but not touched today — pending user decision on priority relative to the other open items. |
| CLOZE-MARKUP-LEAK | ✅ FIXED 2026-06-20 (PHASE2 `a1b9295` / PHASE1 `c8e743a`) — JXA-verified, not yet live-browser-validated | 2026-06-20 — user-supplied conversion files, code-verified: app had zero existing handling | User supplied 4 real Cloze-converted decks for cross-validation. Checked each programmatically for literal `{{cN::...}}` Anki markup leaking into a displayed field: `MGH_Whitebook_cloze_100.json` 0/100, `UWorld_ABIM_cloze_100.json` 3/100, `MGH_WHITEBOOK_2023_2024_FIRST100_CLOZE_cozy.json` 0/100, **`UPDATED_2024_UWORLD_ABIM_FIRST100_CLOZE_cozy.json` 100/100** — every card in that file would show literal `{{c1::CT angio}}`-style syntax in the app. Confirmed via grep the app had **zero existing code** to strip or resolve cloze markup anywhere — `cloze_source_text`/`cloze_enabled` were preserved as metadata only, never sanitized out of the fields actually rendered. Distinct, narrower, and more clearly a real code gap than `DECK-FIELD-DUPE-TEST106` above (which is genuinely data, not code) — this is markup syntax that should never be user-visible, regardless of which deck or which AI did the conversion. | Added `window.stripClozeCardFields351`, a small pure/idempotent helper (`{{c\d+::text(::hint)?}}` → `text`) applied across 18 text fields. Wired into the 3 confirmed array-assignment convergence points for the deck (`setAppCards` ×2 — PHASE2 lines ~8375 and ~10847 — and `setCards` ~line 8674) rather than chasing which of the ~5-7 competing import-pipeline entry functions is actually live for a given upload path — same strategy as the DATA-EO-ALIAS fix (patch the few confirmed funnel points, not every writer). Safe to apply at multiple points because the function is pure and idempotent (a no-op on already-clean text, same single-pass result run twice) — unlike the FQ-ALGO-9/DATA-EO-ALIAS fixes, there's no order- or timing-sensitivity here. Verified via 6-scenario JXA simulation before applying: multi-blank no-hint resolution, hint-form resolution, clean-text no-op, double-pass idempotency, no-throw on null/number/array/undefined fields, multiline cloze spans. Did not touch FSRS/rating/event code. **Not live-browser-validated** — recommend re-importing `UPDATED_2024_UWORLD_ABIM_FIRST100_CLOZE_cozy.json` (the 100/100 file) in a real browser and confirming zero `{{c` strings appear anywhere in the rendered reveal/Full Card/choice text. |
| FULLCARD-COMPLETENESS | ✅ FIXED 2026-06-20 (PHASE2 `a146e46` / PHASE1 `32a0f13`) — JXA-verified, not yet live-browser-validated | 2026-06-20 — re-grounded in `ULTIMATE_GOALS.md`'s 14-field canonical allowlist + `GOAL.md`'s E8 precedent before designing anything | After Codex's live CDP re-test re-confirmed `DECK-FIELD-DUPE-TEST106`'s symptom and proposed a similarity-based suppression fix, user explicitly redirected: function like the current display, show the FULL uploaded JSON content, no hiding/merging, no glitches. Re-read `ULTIMATE_GOALS.md` (the 14-field `canonicalizeCard` export allowlist treats `educational_objective`/`board_trigger`/`explanation`/`why_not_others` as distinct legitimate fields) and `GOAL.md` (E8: "Full Card showed LEVEL 1/LEVEL 2 — whitelist formatter applied" — the established, already-proven pattern for Full Card is a fixed whitelist, not heuristics). Verified the actual live render target first: `ensureFull()`'s "Full Card ▼"/"UP TO CLOSE" dropdown calls `(window.full\|\|full)(...)`; `window.full` has exactly one assignment in the file, to `sourceFull` — confirmed, not assumed. Found `sourceFull()` OR-collapsed `explanation`/`why_not_others` into one line, silently dropping `why_not_others` whenever `explanation` was also present, even though both survive normalization as distinct values. Also missing: `TEST` (deck/source label) and, for cloze cards, `cloze_source_text`. **Caught a self-introduced bug before applying:** a naive "show both whenever both are non-empty" rule would have manufactured a brand-new literal duplicate-text glitch, because `normalizeSourceCard` sets `why_not_others = first(c.why_not_others, explanation)` — meaning when a card has ONLY `explanation` (no separate `why_not_others` in source), `why_not_others` becomes a copy of `explanation` after normalization, and showing "both" would print the same paragraph twice under two headings. | Added an `explSplit` check: show `EXPLANATION` and `WHY NOT OTHERS` as two lines only when they are genuinely different text after normalization; otherwise preserve today's single combined line exactly. Added `TEST` and `CLOZE SOURCE` lines, shown only when present. Reveal screen completely untouched. **Cross-repo divergence found and preserved, not silently resolved:** PHASE1's `sourceFull()` already had `LEVEL 1`/`LEVEL 2` lines that PHASE2 doesn't (likely GOAL.md's E8 fix was applied to PHASE2 only, never ported back) — kept them in PHASE1 exactly as-is rather than deleting as a side effect of porting this fix; flagged in the PHASE1 commit message rather than acted on either direction. JXA-verified across 8 scenarios including the explanation-only/why_not_others-only trap cases. **Not live-browser-validated.** |
| SPARSE-CARD-DIAGNOSIS-POLLUTION | ✅ FIXED 2026-06-20 (PHASE2 `e5e6f6d` / PHASE1 `0ee3bf8`) — Codex live-browser-confirmed, fix JXA-verified, fix not yet re-validated live | 2026-06-20 — Codex retested through the actual Upload button + browser file chooser, not direct function injection | Claude initially concluded (in this same session) that the live JSON-upload path was clean, based on tracing `importObjectPhase3` → `normalizeCardIdentity` → `setAppCards`, which genuinely never touches `educational_objective`/`quick_recall`/`explanation`. **This conclusion was incomplete, not wrong about that one path — Codex's real-upload re-test proved a second, independent path also exists and is live.** A sparse card (`diagnosis` only) uploaded through the real Upload button came back with `educational_objective`, `quick_recall`, `explanation`, AND `level_4_full_card` all set to the literal diagnosis text. | **Root cause traced and confirmed:** a *separate*, older `wire()` (PHASE2 ~line 8565, distinct from the `wire()` that owns `normalizeSourceCard`) attaches its own capture-phase `change` listener to the same 5 upload input IDs and unconditionally calls `normalizeDeck()` (~line 8445) on every invocation (no idempotency guard) — re-mapping the entire live `cards` array through `normalizeLimitlessCard` (~line 8396). That function's `reveal` fallback chain bottoms out at `c.diagnosis`, and that value gets spread into 4 separate output fields. Depending on exact timing of which of the file's competing input-ownership systems holds the element at upload time, this older path can run and retroactively pollute cards `importObjectPhase3` had just stored cleanly — the same class of multi-listener race as FQ-ALGO-9, just for `change` events instead of `keydown`. **Did not touch the listener-ownership race itself** — that needs live event-order instrumentation before any fix, same as FQ-ALGO-9 did, not a guess. Fixed the actual data-fabrication bug instead, at its confirmed source: added `revealSoft` (the same chain, minus the trailing `,c.diagnosis`) and pointed `educational_objective`/`educational_obj`/`quick_recall`/`explanation`/`level_4_full`/`level_4_full_card` at it. `output`/`answer`/`diagnosis` themselves untouched (diagnosis-like fallback is appropriate there); the existing, separately-accepted `board_trigger`→`educational_objective` fallback is preserved exactly. This is the 2nd confirmed-live site fixed for this anti-pattern (1st was the `reveal()`-chain fix for DATA-EO-ALIAS, 2026-06-19). **At least 6 more occurrences traced today and confirmed NOT live for JSON uploads** (CSV-only: `normalizeCard` ×2 + the `normalizeDeck(arr)` that calls one of them; dead code: `normalizeDeckFields352`, zero call sites; export-only: `canonicalizeCard`, only ever called with `mode:'export'`) — left alone, consistent with the standing rule against bulk/unconsolidated patching. JXA-verified across 10 scenarios (sparse-card de-pollution, zero regression on fully-populated cards, board_trigger fallback preserved) before applying. **Not live-browser-validated** — recommend Codex re-run the exact real-upload reproduction against this fix.

**UPDATE 2026-06-22 — level_N removal now complete across all 3 confirmed-live normalizers, live-validated; PHASE1 commit gap caught and fixed:** Codex's live retest of `a0ab37b` found `normalizeLimitlessCard` (~8394) still synthesizing `level_3_recall`/`level_3_quick_recall`/`level_4_full_card` from clean v3 input — that function hadn't received the same complete (same-concept-alias-inclusive) removal already applied to `normalizeCardFields352`/`normalizeSourceCard`. Fixed (PHASE2 `b76b782` / PHASE1 `6eaec1e`), JXA-verified against the legacy-only and full-v3 fixtures, live-confirmed deployed at PHASE2 v44. Separately, Codex caught that PHASE1's equivalent edits had been made but never committed/pushed in the prior round — fixed immediately (PHASE1 `3302d75`/`6eaec1e`), now in sync with PHASE2. **Still open, confirmed by the same live retest, now the top-ranked visible glitch:** reveal duplicate-trigger rendering / DOM churn (54 mutations on a board-trigger-only card, board trigger shown twice) — this is the same already-documented `REVEAL-TRIGGER-CHURN` issue from 2026-06-19 (`ensureBoard()` ~6746 and `renderRevealSections()` ~8896 both independently write to `.boardTrigger350` with different idempotency keys), not a new bug. Deliberately not fixed tonight — same reasoning as 2026-06-19's deferral, this needs its own dedicated consolidation pass, not a patch tacked onto an already-large session. Export confirmed clean across multiple Codex passes now (`cleanDeckPayload`/backup/full-game-state all return zero `level_` keys).

**UPDATE 2026-06-22, same day — found and fixed a 4th contamination site; "fully gone" correctly challenged before being taken at face value:** Codex tested a sparse-but-valid v3 fixture (diagnosis + presentation + board_trigger, no educational_objective/quick_recall) and found `educational_objective` still got populated — pushed back explicitly on Claude's "fully gone" framing, correctly, since only fully-populated fixtures had been re-verified. Traced it to a 4th, entirely separate function never touched in this stretch: a script block (`v1751528-final-js`) that labels itself "Authoritative mapping... LOCKED" and overwrites `window.getPrompt`/`getRevealText`/`getAnswerChoiceText`/`makeChoices`/`nextCard`. Its `revealFor()`/`promptFor()`/`normalizeCard()` trio still read/wrote `level_1_presentation`/`level_2_three_second_exposure`, triggered via `boot()`→`normalizeDeck()` at page load. Fixed (PHASE2 `ffdd76e` / PHASE1 `2cc1a35`), JXA-verified against the exact board-trigger-only repro, a fully-empty card, and a fully-populated card (zero regression — `revealFor` checks `educational_objective` first, so already-good cards self-reassign as a no-op). **Four sites fixed total now: `normalizeCardFields352`, `normalizeSourceCard`, `normalizeLimitlessCard`, and this `v1751528-final-js` trio.** Per the user's own standing caution, NOT claiming "fully gone" again without qualification — verified for the specific fixtures tested (sparse v3, board-trigger-only, fully-empty, fully-populated), not exhaustively for every possible field-combination. Codex's broader retest also surfaced 3 more findings, clustered as likely the same underlying multi-writer issue rather than 3 new bugs: reveal DOM churn measured even higher on a normal (non-board-trigger-only) card (302 mutations, plus an unprompted title change from "Gate Completed" to "Learning Moment" ~1s after reveal with no new user action), stale mixed reveal content surviving a Space-advance to the next card, and `window.current` observed disagreeing with the rendered question/reveal in a 3-card sequence. None fixed tonight — logged as a cluster for the next dedicated reveal-consolidation pass (see `REVEAL-TRIGGER-CHURN` and `AGENTS.md`'s current task).

**CORRECTION 2026-06-22 — the "zero call sites" claim above for `normalizeDeckFields352` was WRONG, and the fix above was incomplete:** Codex's real-upload retest found the pollution still present. Re-checked directly: `normalizeDeckFields352()` IS called from 3 places, and itself calls `cards.forEach(normalizeCardFields352)` — a bare function reference with no `(` immediately following the name, which is exactly the pattern `grep "normalizeCardFields352("` cannot match. Real grep blind spot, not a disagreement. Separately, the `revealSoft` fix above removed the literal `,c.diagnosis` term but left `c.back`/`c.answer`/`c.output` in the same chains — those are themselves diagnosis-equivalent once other normalizers default `answer=diagnosis`. Codex fixed both (PHASE2 `ff98200`, **live-confirmed at v41**); ported to PHASE1 (`c054cab`). Then the user pointed out the deeper architectural fact: none of this contamination chain (`level_2`/`level_2_three_second_exposure`/`level_3_recall`/`level_3_quick_recall`/`card.reveal`, the last confirmed never assigned anywhere in the file) exists in the current Prompt AI v3 schema at all, and `ULTIMATE_GOALS.md` already decided 2026-05-27 these are "derived aliases — must NOT appear." Checked all 6 real deck files examined this session: zero use any `level_` field. Removed these confirmed-dead legacy terms from the two chains above (PHASE2 `80a7a23` / PHASE1 `37685e6`). ~100 more `level_N` references remain elsewhere (CSV-import parsing, `canonicalizeCard`, the field storage itself) — deliberately not touched, separate larger cleanup. |

**2026-06-22 correction to SPARSE-CARD-DIAGNOSIS-POLLUTION:** Codex re-ran the real Upload button/file-chooser test against live PHASE2 and proved the `e5e6f6d` fix was incomplete. Removing the literal `diagnosis` tail from `normalizeLimitlessCard()` still left `back`/`answer`/`output` in its soft fallback chain, and a second live post-import mutator, `normalizeCardFields352()`, still copied `answer` into `educational_objective`, `quick_recall`, and `level_2_three_second_exposure`. Fixed by making both confirmed live normalizers source-preserving for optional learning fields: `answer/back/output/diagnosis` may still feed answer/diagnosis/output, but not educational objective, quick recall, explanation, level 2, or level 4. Local real-browser upload validation passed after the patch: sparse card keeps optional fields empty; explicitly populated TEST_106 fields are preserved. `TEST_106` still has high EO/board/quick overlap because those fields are explicitly near-duplicate in the source JSON.

---

## NON-BLOCKING NOISE

| ID | Status | First Found | Description |
|----|--------|------------|-------------|
| 404-ASSET | ⬛ BY DESIGN | 2026-06-18 browser | 404 is only /favicon.ico — non-blocking, no PWA install impact. |
| D5-SW | ✅ ongoing | 2026-06-16 | SW cache stale if CACHE string not bumped. Managed: bump on every code-change commit. |
| PAGES-JEKYLL-PHASE2 | ✅ fixed 2026-06-22 | GitHub Actions screenshots + public API: PHASE2 built-in Pages runs #83-#93 failed at `Build with Jekyll`; live site stayed on SW v37 despite `main`/`public` containing v40+ fixes. | Root cause class: branch-based GitHub Pages was running Jekyll over a single-file static app repo with many markdown/source artifacts, including Liquid-looking medical/cloze text. Added root `.nojekyll` so Pages serves static files directly. Removed the unpushed custom Pages workflow from the final tree to avoid introducing a second deployment path while Pages is still configured for branch deploy. |
| PAGES-JEKYLL-PHASE1 | ❌ STILL STUCK as of 2026-06-24 — `.nojekyll` present, settings re-save attempted, live site unmoved | Same symptom independently reproduced: PHASE1 live site regressed to SW v65 after a Pages Source disable/re-enable cycle and stayed there through a nudge push, with no `.nojekyll` present (same gap PHASE2 had). Almost certainly the same Jekyll-build-failure mechanism — this repo's own markdown tracking docs contain the same kind of `{{c1::...}}`-style cloze text examples that likely broke PHASE2's Jekyll build. | Applied the identical fix: added root `.nojekyll` (PHASE1 `6906781`). Did not also add an Actions workflow (already tried once, reverted on PHASE2 to avoid a second competing deploy path — same reasoning applies here). Pushing `.nojekyll` alone has not yet triggered a fresh build as of this entry — PHASE2 additionally needed a manual Settings → Pages → Source re-save (branch/`/root`) to actually kick the stuck deploy loose; PHASE1 likely needs the same one-time manual step. **2026-06-24:** alongside switching PHASE2's Pages source from `public` to `main` (separate, unrelated change — see ARCHITECTURE section), user also re-saved PHASE1's Pages source (already `main`→`main`, a no-op value). Polled the live site for 90s after: still serving `cozy-arcade-v65`, local `main` is at `v85`. A same-value re-save likely isn't registering as a change to GitHub's Pages pipeline, so no rebuild triggers. **Next step, not yet tried:** toggle the Pages source to "None" (or a different branch), save, wait ~10s, then switch back to `main` and save — a genuine value change, not a re-confirm, matching what unstuck PHASE2's identical issue on 2026-06-22. |

---

## TESTING INFRASTRUCTURE GAPS

| ID | Status | First Found | Description |
|----|--------|------------|-------------|
| LIVE-NO-DECK | 🔍 monitoring | 2026-06-17 Codex STEP1 | Public GitHub Pages URL has 0 cards — any test requiring card flow (FQ-ALGO-3/4, domain timer with orbs) is unverifiable from public blank build. Runtime path exists in source (curl-confirmed) but execution path untested. Needs: seeded deck or user-loaded deck for interactive validation. |
| SW-CLIENT-STALE | 🔍 monitoring | 2026-06-17 Codex STEP1 | Open browser tabs stay on old cached SW shell until hard reload. curl passes while a tab remains on prior version. Mitigation: always hard-reload before validating. Not a code bug. |
| CONSOLE-GLOBAL-HARNESS | 🔍 monitoring | 2026-06-17 Codex STEP1 | Codex browser eval runs in isolated world — `window.runFSRSValidation` / `window.runCozySmokeTests` appear undefined even when defined in page. Mitigation: curl source-marker gate (current approach). osascript alternative available (requires Safari Develop > Allow JS from Apple Events). |
| NULL-DUE-REPAIR-CONTEXT | 🔍 monitoring | 2026-06-17 Codex STEP1 | FQ-ALGO-3 null-due repair only fires on dirty persisted localStorage. Clean public blank state cannot prove mutation. User must validate on their own session (with real deck + prior progress data). |
| HOME-BUTTON-CSS-CASCADE | ✅ FIXED and live-browser-validated 2026-06-23 (Codex), first attempt was genuinely incomplete | 2026-06-23 Codex screenshot + live browser cross-check, then real Chrome retest after each fix attempt | Devtools screenshot showed `#homeTopBtn` with inline `style="display:none"` yet a real, clickable box rendered on screen. | **Claude's first fix (`#homeTopBtn,.homeTopBtn{display:none!important}`, last in source order) was not sufficient — Codex's live retest caught it:** still `display:grid`, 44x44 box, only `visibility:hidden` until the game-shell class applied. Root cause was a genuine gap in applying CSS cascade rules, not just a guess that turned out wrong: among multiple `!important` declarations for the same property, the browser breaks ties by SPECIFICITY first, THEN source order — being last in the file doesn't matter if an earlier `!important` rule has higher specificity. The surviving competing rules were `button#homeTopBtn` (specificity 1,0,1) and `#homeTopBtn.homeTopBtn` (1,1,0); Claude's plain `#homeTopBtn,.homeTopBtn` (1,0,0 / 0,1,0) lost to both. **Corrected (PHASE2 `1dc3983` / PHASE1 `8465ee9`):** rewrote the final rule to `button#homeTopBtn.homeTopBtn,#homeTopBtn.homeTopBtn,button#homeTopBtn,#homeTopBtn,.homeTopBtn{...}` — the first selector alone has specificity (1,1,1), beating every competing rule found. Verified by Codex via real Chrome: computed `display:none`, 0x0 rect, confirmed at startup, after upload, and in-game, both repos. Also re-ran the Space-advance stale-reveal fix in the same pass — still passes. FSRS 17/17, smoke 6/6, both repos. **Independently reconfirmed 2026-06-24** in a fresh retest against `main` 2ff95d2/v49: computed `display:none`, `pointer-events:none`, rect 0x0 at startup and in-game. Old screenshot state (inline `display:none` but computed `display:grid`) did not reproduce. Closed — do not re-fix. |
| HUD-ICON-BLANK / OLD-SETTINGS-LEAK | ✅ CONFIRMED FIXED (a)+(b) only — PHASE2 `b571eb3` / PHASE1 `80cf287`, 2026-06-24. Browser-validated by user: desktop, short-landscape, and iPhone breakpoints all show painted icons + exactly one settings control. (c) is still OPEN, untouched by this commit — see end of Fix column. | 2026-06-24 screenshot: a real `<button title="Undo">` rendering with no visible glyph (the "Undo" text in the screenshot is the native browser title-attribute tooltip, not app-rendered text); an old floating settings circle visible at the same time as the newer in-HUD settings button. Browser-confirmed at 1028x768 in BOTH repos: Undo button visible with computed `::before` content `none`, HUD settings button visible, AND old `#gearBtn` also visible simultaneously. | **Explicit correction from the user: do NOT bundle this under the already-closed `HOME-BUTTON-CSS-CASCADE` row above — same HUD-shell breakpoint neighborhood, different root cause, tracked separately.** Two independent bugs, both confirmed by direct source read in both repos (line numbers verified by grep, not estimated): **(1) Blank icon glyph.** `.hudIconButton371::before{content:attr(data-hud-icon)}` — PHASE2 `index.html:12500`, PHASE1 `:12486` — is the ONLY rule in either file that paints a HUD icon button's glyph, and it exists ONLY inside a media-query gate. Confirmed via grep across the whole file: no unconditional/base `::before` rule exists anywhere else for `.hudIconButton371` in either repo. Buttons are created correctly (`ensureHudButton()`/`iconize()`, PHASE2 ~12894-12906, sets `button.dataset.hudIcon` fine) — the data is always there, only the paint rule is breakpoint-gated. **(2) Duplicate settings ownership.** The newer in-HUD settings button (PHASE2 `:12989`, a thin proxy: `ensureHudButton(hud,'settings',...) => q('gearBtn')||q('settingsBtn').click()`) and the legacy floating `#gearBtn` (its own fixed-position CSS, e.g. `:1929`/`:1945`/`:2033`) can be visible at the same time during gameplay, because the rule that's supposed to hide `#gearBtn` during play — `body.cozyGameShellActive371 #gearBtn,#homeTopBtn{display:none!important}`, PHASE2 `:12506-12508`, PHASE1 `:12492` — is gated inside the same media block as bug (1). **Not implicated:** `window.undoReview` itself (PHASE2 `:13403`, PHASE1 `:13300`) is unrelated to either bug; the Undo button's tooltip is a genuine native title attribute, not app text. **CORRECTION 2026-06-24, caught by Codex's live cross-repo probe, independently re-verified by Claude via direct `sed` read before accepting:** Claude's first pass wrote "both findings hold identically in PHASE1 at the matching line numbers" — **that's wrong.** The two media gates are NOT the same condition across repos: PHASE2's is `@media (max-width:900px), (orientation:landscape) and (max-height:560px)` (`:12343`, an OR — matches on narrow width OR short landscape); PHASE1's is `@media (max-width:900px)` only (`:12329`, confirmed via direct read — no landscape clause at all). Practical effect: at a short-landscape viewport (~1028x500), PHASE2's OR-clause matches, so its icon paints and `#gearBtn` hides correctly there — PHASE2 is only broken at desktop-normal/portrait. **PHASE1 has no landscape clause, so it stays broken (blank icon + dual settings) at short-landscape too**, not just desktop-normal. Lesson: matching line numbers across repos is not proof of matching behavior — always diff the actual rule content, not just confirm a grep hit exists at roughly the same line. **New, PHASE2-specific subfinding (browser-measured, root cause not yet pinned):** end-to-end iPhone-viewport probe (real uploaded deck) found PHASE2's HUD settings button rendering **partially offscreen** — `rect x=-27, w=34` — gear was correctly hidden and icons painted correctly at this viewport, but the settings control itself became mostly inaccessible. Distinct from the desktop blank-icon bug. Candidate CSS area to check next: the narrow-breakpoint `.hudActions371` rules at PHASE2 `:12438`, `:12451`, `:12800`, `:12804` — not yet traced to a specific declaration. **New, PHASE1-specific subfinding (source-confirmed, no PHASE2 equivalent):** PHASE1 has a block PHASE2 entirely lacks — `<style id="vHUD-polish-2">` (`:14518-14521`) — with `.gameHud button[data-home], .hudActions371 button[data-home]{display:none!important}`, **unconditional, not media-gated at all**, permanently hiding PHASE1's in-HUD exit/home button (the one `ensureHudButton(hud,'home',...)` creates and tags `data-home`) at every breakpoint. Confirmed via grep: PHASE2 has zero references to `vHUD-polish-2` or `button[data-home]`. Plausibly related to the already-logged `COZY-HOME-DUPE-MOBILE` differential (PHASE1's `MOBILE_RECTIFIER_PLAN.md`) — not confirmed causally, flagged as a lead for that investigation, not claimed as the fix. | **Not yet fixed — analysis only, no code changed.** Narrow, independent fixes, do not conflate them in one patch: (a) move or duplicate the `.hudIconButton371::before` rule outside its media gate (or render the icon glyph directly via JS instead of a pseudo-element) so HUD icons paint at every breakpoint — same conceptual fix, but PHASE1 and PHASE2 need it applied to *different* media conditions per the correction above, don't copy-paste one repo's exact gate onto the other without checking. (b) Pick one settings owner: either make the `body.cozyGameShellActive371 #gearBtn` hide-rule unconditional if the HUD proxy is meant to be the sole in-game settings entry point, or remove the HUD settings proxy if `#gearBtn` should stay canonical — product-intent choice, flag for the user before picking. (c) PHASE2's offscreen-settings-on-iPhone issue and PHASE1's unconditional home-button-hide need their own separate investigation passes — do not fix opportunistically as a side effect of (a)/(b). **(a)/(b) CLOSED 2026-06-24:** added unconditional (outside any media query) versions of both rules in both repos — `.hudIconButton371::before` and `body.cozyGameShellActive371 #gearBtn{display:none!important}` — committed together as one combined fix (user's explicit call: these were validated together, splitting into two commits would be artificial). Browser-confirmed by user across desktop/short-landscape/iPhone in both repos. **(c) remains OPEN, not touched:** PHASE2 HUD settings button still offscreen on iPhone (`rect x=-27, w=34`, re-confirmed in this same retest); PHASE1's `vHUD-polish-2` unconditional home-button-hide untouched. |

---

## 2026-06-28 SESSION — Knowledge Pulse + oneThing Flash (v50→v56 / v86→v92)

| ID | Status | First Found | Description | Fix |
|----|--------|------------|-------------|-----|
| PULSE-CLICK-BLOCK | ✅ FIXED PHASE2 v53 / PHASE1 v89 | 2026-06-28 source analysis | Toast visible but blocked clicks on underlying cards in Domain mode. Root cause: `pointer-events:none` on `.pulseToast351` was inside `.cozyGameShellActive371` guard; `renderDomain()` calls `document.body.className=''`, stripping that class mid-game. | Moved `pointer-events:none` to base `.pulseToast351` rule, unconditional. |
| PULSE-TOAST-STACK | ✅ FIXED PHASE2 v53 / PHASE1 v89 | 2026-06-28 source analysis | Multiple toasts stacked on rapid correct answers. `showPulseToast()` appended without removing prior ones. | `showPulseToast()` now removes all `.pulseToast351` before appending. Same commit as PULSE-CLICK-BLOCK. |
| PULSE-MILESTONE-ORDER | ✅ FIXED PHASE2 v55 / PHASE1 v91 | 2026-06-28 browser validation (v54/v90) | At 9→10 correct: SYSTEM SURGE shown instead of BOARD TRIGGER UNLOCKED. `checkKnowledgePulse()` `else if` chain was 5→10→25→50 — `n=10` matched `n%5===0` first. Also: PHASE2 `peekNextCard()` fallback had `+1` index offset (PHASE1 already correct). | Reordered chain to 50→25→10→5. Fixed PHASE2 fallback index. |
| ONETHING-PRIOR-FLASH | ✅ FIXED PHASE2 v56 / PHASE1 v92 | 2026-06-28 (prior session regression) | Prior card's One Thing text visible one frame on new card reveal. `wrappedAdvance` cleared `oneThingMeasuredLong351` dataset but left old innerHTML. `renderRevealSections()` deferred via `setTimeout(0)` — one-frame gap. Also: `renderOneThingStable351` trusted stale measurement when card key changed. | `wrappedAdvance` now clears `innerHTML` + `oneThingStableRenderSig` (guarded: skip if textarea/edit active). Measurement guard tightened: trusts `oneThingMeasuredLong351` only when `oneThingStableKey===key`. |
| BOARD-TRIGGER-PREVIEW-TIMING | ❌ OPEN | 2026-06-28 source analysis | `peekNextCard()` reads pre-advance pool. `poolKey()` includes `session.seenThisSession.size` (line 11597). `seenThisSession` updates inside `wrappedRate`, so pool rebuilds + `index` resets to 0 before next card is actually selected. Preview card id at n=10 toast ≠ actual next card dealt. Not yet browser-confirmed as visible symptom. | Needs browser harness: log `peekNextCard().id` vs `current.id` after advance at 9→10 correct. Then decide: pre-compute preview before pool rebuild, or show generic milestone message instead of live card preview. |
| MULTI-OWNER-CLEANUP | ❌ OPEN (documented, low urgency) | 2026-06-28 Codex post-test recommendation | Multiple ownership gaps: competing card import normalizers, no single progress-merge owner, multiple independent reveal writers, localStorage deck restore (`cozy_arcade_limitless_cards_v1`, PHASE2 line ~8430) fires unconditionally on load. | Not assigned. Needs a dedicated cleanup pass: one import owner, one progress owner, one reveal owner. Gate localStorage restore. Do not fix opportunistically. |

---

## 2026-07-03 SESSION — Codex browser test (A–F) + REVEAL-FIRST-FRAME root cause fixed (v60→v61 / v95→v96)

| ID | Status | First Found | Description | Fix |
|----|--------|------------|-------------|-----|
| D4-MUTATION-LOCK | ✅ LIVE-VALIDATED 2026-07-03 | CODEX_PROMPT_18 browser run | 3 Space advances; zero bad next-card selectSolo within 50ms. Mutation deltas 4/6/8 per advance (low, acceptable). Caveat: reveal() path had to be force-called in headless (see ANSWER-SELECT-HEADLESS below) — real-device retest still recommended. | No code change. Commit 562facd/0057270 confirmed working. |
| CLOZE-MARKUP-LEAK-B | ✅ FIXED AND LIVE-VALIDATED PHASE2 v62 / PHASE1 v97 | 2026-07-03 Codex v63 real-browser pass | Full Card CLOZE SOURCE: no raw `{{c...}}` in rendered display. Raw `cloze_source_text` field in card object still contains `{{c...}}` (source provenance preserved). | Display-only strip in `sourceFull()` line 8739 (P2) / 8769 (P1). Commits 80fa071/5039df6. Closed — do not re-touch. |
| FULLCARD-COMPLETENESS-C | ✅ PASS — formatter confirmed | 2026-07-03 Codex sourceFull() probe | `sourceFull()` confirmed: TEST line present, EXPLANATION shown, WHY NOT OTHERS shown separately when distinct, no collapse. Modal click path in headless returned only "Close" element — not a product bug, headless harness artifact. | No code change needed. `a146e46`/`32a0f13` confirmed working via direct sourceFull() call. |
| REVEAL-FIRST-FRAME | ✅ FIXED AND LIVE-VALIDATED PHASE2 v61 / PHASE1 v96 | 2026-07-03 Codex MutationObserver on #soloTrigger — first confirmed then validated in same session | Real mechanism found: `rerenderVisibleBionic351()` (PHASE2 line 6705, PHASE1 line 6739) wrote `soloTrigger.innerHTML = safeBionic(obj)` where `obj = clean(c.educational_objective\|\|...)` — no contamination guard. Bionic boot loop fired at 300ms / 1200ms / every 1000ms after reveal, overwriting correct board_trigger with contaminated EO (= diagnosis text). **Live-validated after fix:** observer before reveal, first #soloTrigger write = "Iron deficiency + diarrhea -> tissue transglutaminase IgA" (board_trigger). Delayed writes (up to 1500ms observed): same board_trigger text. Diagnosis hits: 0. PASS. | Applied contamination guard: `const eduRaw6705=clean(c.educational_objective\|\|''); const obj=(eduRaw6705&&eduRaw6705!==clean(c.diagnosis\|\|''))?eduRaw6705:(clean(c.board_trigger\|\|c.quick_recall\|\|c.objective\|\|c.answer_explanation\|\|c.explanation\|\|''));` — PHASE2 line 6705, PHASE1 line 6739. Commits 7a61368 (PHASE2 v61) / e5c5c6a (PHASE1 v96). Closed — do not re-fix. |
| ONETHING-LONG-E | ✅ PARTIAL PASS — mobile layout ok, cross-card flash needs real-device retest | 2026-07-03 Codex mobile 390x844 | Long One Thing: `oneThingLong351` class applied correctly. Toggle exists. Did not cover rating buttons. No stale card text in observed state. Caveat: harness stayed on same card key (forced reveal path), so cross-card prior flash not fully confirmed. | No code change. v56 fixes hold for observed conditions. Re-test with true cross-card advance on real device or browser with functional answer-selection path. |
| SPARSE-CARD-POLLUTION-F | ✅ FIXED AND LIVE-VALIDATED PHASE2 v63 / PHASE1 v98 | 2026-07-03 Codex v63 real-Upload (real browser file input, SW blocked, FSRS 17/17, smoke 6/6) | Sparse card after upload: `educational_objective` empty, `quick_recall` empty, `explanation` empty. No diagnosis pollution, no board_trigger pollution. `answer`=diagnosis (correct for MCQ). Two-pass fix required: v62 guard `(a!==d)` still wrote `board_trigger` into EO; v63 removed the synthesis entirely — `normalizeCard()` P2 line 5425 / P1 line 5436 now: `c.educational_objective=c.educational_objective\|\|''; c.answer=a; c.quick_recall=c.quick_recall\|\|'';` — EO/QR only from source JSON, never synthesized from `a`. | Commits 29c7e50 (P2 v63) / 718b817 (P1 v98). Closed — do not touch `normalizeCard()` or reveal wrappers further. |
| ANSWER-SELECT-HEADLESS | 🔍 NEW DIFFERENTIAL — not confirmed user-visible | 2026-07-03 Codex harness | In headless Chrome, answer selection did not open reveal even with: real DOM click, forced click, number key (1-4), and direct `selectSolo(idx)` call. Harness had to force `reveal('solo',true)` to proceed. Root cause unknown — may be game readiness debounce, runner overlay, or an event-path issue specific to headless. | Not actioned. Low priority if not user-visible. Confirm on real device before treating as a real bug. Do not re-fix RUNNER-INTERCEPTS-CLICKS (already closed, confirmed working at 390×844 and 375×667). |

---

## 2026-07-04 EMERGENCY — ADVANCE-LOCK-SELF-CANCEL: switching Pages to `main` exposed a real, already-live regression (v64/v65, v99/v100)

| ID | Status | First Found | Browser Evidence | Description | Fix |
|----|--------|------------|-----------------|-------------|-----|
| ADVANCE-LOCK-SELF-CANCEL | ✅ FIXED, LIVE-CONFIRMED (PHASE2 v65 / PHASE1 v100) | 2026-07-04 — user-reported immediately after the deploy fix went live: "card will not advance, stuck after answer" | Live-browser-confirmed both before and after fix: Space/Continue correctly hid the reveal panel but `current`/pool index never advanced — same card stayed on screen every time, both repos, 100% reproducible. Post-fix: 3 full cycles each (correct+Space, wrong+Space, Continue button) on live production, all advanced correctly. | **This was NOT caused by today's AUTO-TAGS-DIAGNOSIS-LEAK fix, the deploy-config change, or any doc/workflow work — it was a pre-existing bug already sitting on `main`, just never live because Pages had been frozen on stale `public` content for 10+ days.** Root cause, confirmed via live browser trace (Claude's local repro + Codex's live instrumentation converged independently): `wrappedAdvance` (~PHASE2 line 14472) checked and set the shared `__cozyLastAdvanceAt351` 350ms lock **unconditionally**, even on calls where the reveal panel wasn't open. A separate, redundant lock in `bindFinalRatings()`'s Continue-button handler (`finalAdvanceLockUntil = Date.now()+220`, set immediately before calling `advance()`) could poison timing such that the real Continue/Space press arrived just after the shared lock had been (incorrectly) set by an unrelated/non-reveal call, and got silently swallowed — `advance()` returned before ever reaching `nextCard()`/`renderSolo()`. Fixed by (1) removing the redundant pre-lock in `bindFinalRatings()`, (2) gating both the 350ms check AND the timestamp-set in `wrappedAdvance` on `revealOpen===true`, so the lock can only ever apply to genuine duplicate reveal-dismissal calls, never to unrelated ones. PHASE2 `933a74e` (v64→v65), PHASE1 `3361061` (v99→v100). Validated via full local Playwright matrix (correct+Space, wrong+Space, Continue button, FSRS 17/17, smoke 6/6) before push, then **live-re-confirmed on production** after push: 3 cycles each condition, both repos, all pass. | **Standing lesson, add to the deploy-verification protocol:** fixing a stale-deploy problem can surface bugs that have been silently accumulating on `main` the whole time it wasn't live. Do not assume "the deploy is fixed" means "the app is fine" — run a real functional smoke pass (not just a version-number curl check) immediately after any deploy-pipeline fix, before considering the incident closed. |

**2026-07-04, same emergency, Codex live-diagnostic sweep of 4 items (production, both repos) after the fix above:**
1. **Advance/reveal lock recurrence — PASS.** 3 cycles each (correct+Space, wrong+Space, Continue), both repos. Confirms `ADVANCE-LOCK-SELF-CANCEL` fix holds live, not just locally.
2. **REVEAL-TRIGGER-CHURN — still real, unrelated to this incident.** PHASE2: 91 mutations at reveal + 44 after advance = 135. PHASE1: 90 + 46 = 136. Consistent with this differential's existing history (89–620 depending on measurement conditions) — this emergency did not fix or worsen it. Remains the top-ranked real open architecture bug, still needs its own dedicated consolidation pass, not a reactive patch.
3. **One Thing stale/long state — PASS for the tested path.** 4 live cycles, both repos, no stale prior-card content detected, long-text class applied correctly. Still recommend a real-device retest per the existing `ONETHING-LONG-E` caveat (headless/forced-reveal harnesses can miss true cross-card timing).
4. **`current === null` renderSolo edge (Claude's finding, see below) — real bug, NOT naturally reproduced.** Natural single-card and multi-card live cycles: no console error, no crash. Only reproduced via forced injection (`current = null; renderSolo()`) → confirmed throws `Cannot read properties of null (reading 'system')` at PHASE2 ~3953 / PHASE1 ~3964. **Verdict: keep as a documented differential, not an emergency.** Do not patch until either a natural browser path produces `current === null` or a live console genuinely shows this exact error — this is a real defensive-coding gap (worth fixing eventually, low risk to fix) but is confirmed NOT to be what caused today's user-facing incident, which is fully explained by `ADVANCE-LOCK-SELF-CANCEL` above.

**Priority going forward (Codex + Claude converged):** advance-lock fix stays closed unless the exact stuck-card symptom recurs. Next real target is `REVEAL-TRIGGER-CHURN`. One Thing: monitor only. `current===null`: valid low-priority defensive-patch candidate, not urgent. HUD/iPhone work: deferred until reveal-churn is addressed, per standing "no quick patch" practice — don't context-switch onto a new area while a known architectural bug is still open.

**2026-07-04, same day, user played it live and confirmed advance works; Codex reran items 1–4 and added a 5th (HUD), correcting the prior HUD diagnosis:**
1. **Advance lock — PASS, both repos, user-confirmed by actual play** (not just automated cycles this time). `ADVANCE-LOCK-SELF-CANCEL` stays closed.
2. **Reveal churn — FAIL, still real.** PHASE2: 91 mutations at reveal + 44 after advance = 135. PHASE1: 95 + 46 = 141. Consistent with prior measurements — confirms `REVEAL-TRIGGER-CHURN` as the correct next target, not a regression from today.
3. **One Thing — PASS**, holds on rerun.
4. **`current===null` — still forced-only**, not naturally reproduced. `RENDER-NULL-CURRENT` status unchanged.
5. **HUD — FAIL, but the diagnosis itself was corrected, superseding the stale "(c) offscreen gear" framing in `HUD-ICON-BLANK / OLD-SETTINGS-LEAK` above.** Live browser findings, both repos: (a) the home gear button is NOT offscreen — visible, on-screen, 44×44, top-right, both repos; (b) clicking gear opens the settings **drawer** (`cozyDrawerOpen351` body class), not the `#settings` screen — possibly intended, but the drawer can intercept clicks if left open, worth a product-intent check later; (c) the in-HUD Settings button is genuinely present and visible in both repos (PHASE2 ~x=153,y=45; PHASE1 ~x=183-198,y=87) — Settings is not missing, it's just in-HUD, not the old floating gear; (d) **new, real, PHASE1-only finding:** PHASE1's Solo HUD has **duplicate anonymous Home buttons partially offscreen** (`x=-5` or `x=-13`, width 44) — distinct from `#homeTopBtn`, which IS already correctly hidden; do not re-fix `#homeTopBtn`, the bug is in whatever generates these anonymous duplicates. (e) PHASE1's HUD layout is more cramped than PHASE2's — Energy/Score/Streak compressed into a left rail (~x=3-10, width 16-18px) vs. PHASE2's cleaner top layout; do not port PHASE1's HUD CSS into PHASE2 as a "fix," it's the worse of the two. **Not patched — explicit decision, both Codex and Claude agree: do not touch HUD now unless the user explicitly asks. If/when it is touched, target the duplicate anonymous HUD home-button generator (PHASE1), not `#homeTopBtn` (already correctly hidden) and not the settings-button-visibility question (already confirmed fine).**

---

## 2026-07-04 SESSION — AUTO-TAGS-DIAGNOSIS-LEAK fixed (v64/v99); deploy confirmed still stale; new multi-owner finding

| ID | Status | First Found | Browser Evidence | Description | Fix |
|----|--------|------------|-----------------|-------------|-----|
| AUTO-TAGS-DIAGNOSIS-LEAK | ✅ FIXED, COMMITTED, PUSHED, AND LIVE-CONFIRMED PHASE2 v64 / PHASE1 v99 | 2026-07-04 — Codex real-Upload retest of v63 found it, Claude source-verified before fixing | Codex confirmed via real Upload button (`#limitlessHomeDeckFile`), sparse card, SW blocked + SW allowed, both cases: sparse card Full Card TAGS line now reads `UNSPECIFIED` — no diagnosis. FSRS 17/17, smoke 6/6. **Live-confirmed same day** via direct curl of both production `sw.js` files after the DEPLOY-STALE-GATE incident (below) was resolved: PHASE2 `cozy-arcade-PHASE2-v64`, PHASE1 `cozy-arcade-v99`, both matching local exactly. | Three confirmed-live diagnosis→tags fallback sites found via full-file grep of every `.tags` reference (not a partial check): `normalizeCard()` (~5427, CSV-import path per call-site trace), `normalizeLimitlessCard()` (~8411, CSV-import path), `normalizeSourceCard()` (~8701, JSON-upload path, called inside `cozy-source-mapping-ui-20260517`). All three changed `[system,test,diagnosis]` → `[system,test]`, matching the pattern two sibling normalizers (lines 1800/4735) already used safely. Full-file re-grep after the fix (`.tags\s*=` filtered for diagnosis/dx) returned zero matches in both repos. PHASE2 `a1fea48`, PHASE1 `5ed3f60`. |
| WINDOW-NORMALIZE-LIMITLESS-TRIPLE-OVERWRITE | 🔍 NEW, confirmed by source, not yet actioned | 2026-07-04 — found while tracing whether the `normalizeLimitlessCard()` tags fix above was actually live for the real upload path | `window.normalizeLimitlessCard` is assigned THREE times across three separate script blocks, in document order: (1) `limitless-card-id-import-patch` line ~8432, `=normalizeLimitlessCard` (the function this session patched for tags); (2) `cozy-source-mapping-ui-20260517` line ~8931, `=normalizeSourceCard` (a DIFFERENT function, confusingly assigned to the same global name); (3) `cozy-public-v18-drawer-js` line ~9006, an inline arrow function with its own independent EO/answer/diagnosis logic — this is the one actually live for anything calling `window.normalizeLimitlessCard` (e.g. `setCards351` ~9055), since script tags execute in document order and this is the last assignment. Confirmed this 3rd version never touches `.tags` at all (spread preserves whatever was there) — which is WHY Codex's real-upload test came back clean even before today's fix: the live path was never exposed to this specific bug. The raw `normalizeLimitlessCard` function this session fixed is only reachable via its CSV-import call site (~8481), not the JSON-upload path. | Not actioned — no bug reached the user through this specific mechanism today. Flagging so a future session doesn't waste time "fixing" the wrong `normalizeLimitlessCard` again: if a future differential needs the JSON-upload-live version, edit the arrow function at ~9006, not the named function at ~8393. Same class of confusion as the already-documented multi-owner `reveal()`/`advance()` chains — add to AGENTS.md's chain-map table. |
| DEPLOY-STALE-GATE | ✅ ROOT CAUSE FOUND AND FIXED 2026-07-04, STANDING RULE ADDED — this cost a full day twice (a prior week-ago incident, then again today) before being root-caused | User explicitly flagged this had already wasted a full day once before, then wasted another full day today, and demanded a permanent fix, not just a rule to "remember to check." | **Full root cause, finally confirmed empirically (not from docs, which were wrong):** PHASE2's GitHub Pages **Source** setting (Settings → Pages → Build and deployment → Source) was actually still `public`, despite this project's own docs (`PAGES-SOURCE-BRANCH`, 2026-06-24 entry above) claiming it had been switched to `main`. That switch either never actually saved or was silently reverted — nobody caught it because nobody re-verified it empirically afterward, they trusted the doc. Proof: `git show origin/public:sw.js` matched the live site exactly (v49) while `origin/main` had raced ahead to v64 over ~15 commits and 10+ days with zero live effect. **Compounding wrinkle found mid-fix:** even after force-pushing `main`'s content into `public` to test a workaround, GitHub's own internal classic "pages build and deployment" pipeline's *build* step succeeded but its *deploy* step failed, twice, identically — most likely an Environment (`github-pages`) deployment-branch-restriction left over from the abandoned 06-24 switch attempt, restricting deploys to `main` only while Source still pointed at `public`. **Actual fix:** user changed Settings → Pages → Source from `public` to `main` directly. Live matched local within one polling cycle (~10s) after that change — confirmed via curl immediately after. | **Two permanent standing rules, not just "remember to check":** (1) **Never trust this project's own docs about which branch Pages deploys from — a doc saying a Settings change was made is not proof it stuck.** Before ever declaring a deploy-config differential closed, re-verify with `git show origin/public:sw.js` AND `git show origin/main:sw.js` AND a live curl, all three, and confirm which one the live site actually matches — that tells you the true Source, regardless of what Settings claims or what a prior session's notes say. (2) As a structural safety net (not just a check), both repos now also run `.github/workflows/pages.yml` ("Sync main to public") on every push to `main` — force-pushes `main`'s content into `public` automatically. This means regardless of which branch Pages Source is actually set to (even if it silently reverts again), the live site can never be more than one push-to-`main` behind, because `public` is now always kept current too. Keep this workflow running indefinitely (explicit user decision 2026-07-04) — it's cheap insurance against this exact failure mode recurring a third time. |
| PHASE1-HUD-DEDUPE-MISSING | ⚠️ CORRECTED same day — the "port patchRenderHudHooks" diagnosis below was WRONG; real root cause found and FIXED, both repos, by DELETING code (see PHASE1-HUD-DUPLICATE-CREATORS below) | 2026-07-04 static diff, PHASE2 vs PHASE1, following up the "PHASE1 duplicate anonymous Home buttons" live finding | Original analysis (preserved per this file's rules, not deleted): grepped for `patchRenderHudHooks`/`__e7cHud371`, found zero hits in PHASE1, concluded the dedupe wiring was simply missing. **This was incomplete — PHASE1 DOES call `renderHudControls` via its `normalizeHud` alias**, inside `run()`, on a `setTimeout(run,100)`/`,700`/`setInterval(run,4000)` schedule — the direct-name grep missed it, the exact same bare-reference-vs-alias trap this project has hit before (`normalizeCardFields352`, 2026-06-22). The REAL problem wasn't missing dedupe wiring; it was two separate legacy functions (`addHudHome`, `standardHud`) each independently CREATING their own home button, which the slower (4s-interval) dedupe sweep only cleans up after a delay — see `PHASE1-HUD-DUPLICATE-CREATORS` for the actual root cause and fix. | Do not port `patchRenderHudHooks` to PHASE1 — that would have been adding a 3rd/4th wrapper layer to paper over a problem that a 2-function deletion actually solves. Standing lesson, repeated: before concluding "X is never called," grep the bare name, not just `X(`. |

| PHASE1-HUD-DUPLICATE-CREATORS | ✅ FIXED, BOTH REPOS, BY DELETION NOT PATCHING 2026-07-04 (PHASE2 v66→v67, PHASE1 v101→v102) | 2026-07-04, user explicitly pushed back on "port a fix" as the plan and asked "why can't you reduce code bulk instead of patching" | Live-instrumented PHASE1's Solo HUD at a real mobile viewport (390×844): found 5 buttons where 4 should exist — an untagged `.hudHome1751511`/`hudPill` button (text `⌂`) at `x:-5` (offscreen), created by the legacy `addHudHome()` function, alongside the canonical `data-hud-role="home"` button (created by `ensureHudButton`/`renderHudControls`, itself invisible — rect `{0,0,0,0}`). Checked PHASE2 for the same legacy function and found **it had already been fixed**: `addHudHome()`'s button-creation block is deleted in PHASE2, replaced with a one-line comment ("Home button injection disabled — normalizeHud() is the sole owner"), keeping only the harmless pause/exit relabeling calls. A second legacy creator, `standardHud()` (creates a near-identical `.hudHome1751512` button, checks for either old class before creating its own), had the identical fix already applied in PHASE2 too. **Both deletions ported to PHASE1 verbatim**, matching PHASE2's already-production-proven versions exactly — net negative lines, zero new wrapper/hook logic. **Second bug found while testing the fix, before considering it done:** with both legacy creators gone, the ONLY home button left is the canonical one — and it was invisible in BOTH repos at mobile viewport, because a CSS rule (`.hudActions371 button[data-home]{display:none!important}`, media-gated to ≤900px/landscape in PHASE2, unconditional in PHASE1's `vHUD-polish-2`) was written specifically to hide it, back when the legacy button was providing the visible affordance. **This is a real, previously-undiscovered bug already live in PHASE2 production** (not something introduced today) — PHASE2's Solo HUD has had no visible home/exit button on mobile viewports, full stop, until this fix. Removed the now-purposeless hide rule in both repos. | **Full validation before commit, both repos:** live Playwright test at 390×844 confirms exactly 4 buttons (Undo/Pause/Home/Settings), all on-screen, none offscreen, none duplicated; clicking the home button correctly transitions Solo→Home screen; `runFSRSValidation()` 17/17, `runCozySmokeTests()` 6/6, zero page errors, both repos. **Deliberately left alone:** ~6 lines of now-fully-dead CSS in PHASE1 sizing the deleted `.hudHome1751511`/`1751512` classes (PHASE2 already carries 2 equivalent dead lines and never bothered removing them either — matching established practice, not re-litigating it today). **This is the shape "reduce bulk instead of patching" should take**: found the actual duplicate-generating code (not a wrapper to clean up after it), confirmed PHASE2 had already proven the exact deletion safe in production, applied it verbatim, then caught and fixed a second, real, previously-unknown bug the deletion exposed — rather than stopping at "duplicates gone, ship it." |

| HUD-DOUBLE-ICON-PAINT | ✅ FIXED, BOTH REPOS, BY DELETION 2026-07-04/05 (PHASE1 v102→v103, PHASE2 v67→v68) | 2026-07-04 — user's real-device screenshots (`/Users/rebekahbetar/Documents/glitch source/HUD GLITCH/`) taken AFTER `PHASE1-HUD-DUPLICATE-CREATORS` was already live, showing the Home icon still cycling 0/1/2 across rapid screenshots; separately, Codex's independent live browser audit of PHASE2 (2026-07-05) found the identical mechanism there | Live-instrumented computed styles (`getComputedStyle(btn,'::before')` AND `'::after'` — checking only `::before` initially missed this) on `#soloPause` and the `[data-home]` exit button, both repos: each had TWO simultaneously-active icon-painting mechanisms — an old, hardcoded `#soloPause::after{content:'Ⅱ'}` / `[data-home]::after{content:'×'}` rule predating the current icon system, stacking on top of the newer, unconditional `.hudIconButton371::before{content:attr(data-hud-icon)}` rule. Undo and Settings buttons (no legacy class) rendered clean with only one pseudo-element — direct proof the newer system alone is sufficient, confirming the old rules are pure redundant duplication, not a real fallback need. | Removed only the two old `::after{content:...}` declarations in both repos; deliberately left the accompanying `font-size:0` text-hiding rules untouched (verified via live computed-style check they are unconditional and correctly hide real text at both mobile 390×844 and desktop 1280×900 viewports, before and after the change — not the cause, no reason to touch). PHASE1 `56fc68d` (fixed 2026-07-04, this session), PHASE2 fixed 2026-07-05 after explicit user confirmation to lift the same-day "no more PHASE2 changes" restriction for this one fix, matching Codex's independent audit recommendation. **Full validation both repos:** `afterContent` computed to `"none"` on every HUD button, single clean icon render, home-button click still correctly transitions Solo→Home, `runFSRSValidation()` 17/17, `runCozySmokeTests()` 6/6, zero page errors. |

| NO-SYNTHESIZED-LEARNING-FIELDS | ⬛ STANDING RULE, added 2026-07-04 | Root pattern behind DATA-EO-ALIAS, SPARSE-CARD-DIAGNOSIS-POLLUTION (×2), and AUTO-TAGS-DIAGNOSIS-LEAK — same anti-pattern recurring across 3+ separate sessions in different fields | Every occurrence of this bug class followed the same shape: a normalizer defaults an empty learning-display field to the diagnosis/answer text as a "better than blank" fallback, which then displays as duplicated/contaminated content once the field is shown standalone. | **Standing rule for all future normalizer code in this file:** diagnosis/answer/output MAY be used to synthesize `answer`/`diagnosis`/`output` themselves (that's correct, expected MCQ behavior). Diagnosis/answer must NEVER be synthesized into `educational_objective`, `quick_recall`, `explanation`, `one_thing`, `board_trigger`, or `tags` — leave those empty/absent when the source JSON doesn't provide them, per `FULLCARD-COMPLETENESS`'s "show what's there, don't manufacture content" precedent. Before adding any new normalizer or fallback chain touching these fields, grep the full file for the field name first (not just the function you're editing) — the multi-owner history in this project means a clean fix in one normalizer is not proof the field is clean everywhere. |

---

## STANDING REGRESSION CHECK — sparse-card learning-field/tags contamination

Added 2026-07-04 after the 3rd occurrence of the same bug class (DATA-EO-ALIAS, SPARSE-CARD-DIAGNOSIS-POLLUTION, AUTO-TAGS-DIAGNOSIS-LEAK). Run BOTH of these before closing any future differential in this family — do not rely on one and skip the other, they catch different failure modes (static leak vs. runtime-visible leak):

1. **Static full-file grep (fast, Claude can run this without a browser):** `grep -n "\.tags\s*=" index.html | grep -iE "diagnosis|c\.dx\b"` (and the equivalent for `educational_objective`/`quick_recall`/`explanation`/`one_thing`/`board_trigger` — grep the FULL file, not just the function being edited; the multi-owner history here means other normalizers can carry the same anti-pattern). Zero matches required. Also re-check `WINDOW-NORMALIZE-LIMITLESS-TRIPLE-OVERWRITE` style traps: grep `window\.<functionName>\s*=` for every normalizer touched, to confirm which assignment actually wins at runtime before declaring a fix "live."
2. **Real-browser upload regression (Codex, requires the actual Upload button `#limitlessHomeDeckFile`, not direct function calls):** upload a synthetic sparse card (diagnosis + system + test only, no educational_objective/quick_recall/explanation/tags in source). Assert after render: `educational_objective`, `quick_recall`, `explanation` all empty; Full Card TAGS line does not contain the diagnosis text; no diagnosis text appears anywhere outside the diagnosis/answer/choice UI. Test under both SW-blocked and SW-allowed conditions (per this session's method) since stale cache can mask a real regression as a pass.

## HOW TO USE THIS FILE

**Codex:** Before any fix, check this file for related open items. After browser validation, update the status column and add the commit hash. Do not re-investigate items marked ✗ DISPROVED — they were proven wrong by browser. Run the STANDING REGRESSION CHECK above before closing any sparse-card/tags/learning-field differential.

**Claude:** After each Codex session, append new differentials from the audit report and close confirmed items. Never delete rows. Before reporting any fix as "done," curl the live `sw.js` for both repos and state both versions explicitly — do not equate local-fixed with user-visible-fixed (see `DEPLOY-STALE-GATE`).

**Add a new row:**
```
| NEW-ID | ❌ OPEN | YYYY-MM-DD | [browser evidence] | [description] | — |
```
